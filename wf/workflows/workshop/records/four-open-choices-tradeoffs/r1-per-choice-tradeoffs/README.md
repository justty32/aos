# R1 想法池
← [四個懸而未決的設計選擇](../README.md)

R1 四位各自發想的完整想法池。原本一檔，已按「哪一題」再拆一次：〈成形的方向〉底下的四題各自一檔，
橫跨四題的三段（還在生長的想法／大家問出來的問題／明顯的坑）另成一檔。

| 檔案 | 裡面有什麼 | 什麼時候會想看 |
|---|---|---|
| [一、`World` 抽象要不要收](q1-world.md) | 收／不收 `World` 的「得到什麼／付出什麼／什麼時候會後悔／能不能反悔」對照表；B 集中 lane 幾乎會逼出 `World`；root fd 不是沙盒的提醒。 | 想知道 `World` 這層抽象現在收、之後補，各自的代價 |
| [二、`kernel.json` 要單層，還是分層合成](q2-kernel-layering.md) | 單層／分層／第三條路（分層撰寫、物化有效內容）三行對照表；`.runi` 只存 hash 為何不夠；外部處理器要不要懂合成規則。 | 想知道 kernel 政策放哪一層、事故要不要能重播 |
| [三、子行程拓樸選 A、B，還是先固定磁碟 ABI](q3-topology.md) | A 完整巢狀／B 集中 lane／C 先固定磁碟 ABI 三行對照表；A 的每層都是事故現場、B 的 root 是共同故障域、C 只能延後誰推進。 | 想知道子行程拓樸三條路各換到什麼、反悔要不要搬資料 |
| [四、親緣綁路徑、綁 UUID，還是把身分／親緣／位置拆開](q4-identity.md) | 綁路徑／綁 UUID／混合（`proc_id`＋`parent_id`＋`location`）三行對照表；UUID 穩住「它是誰」卻沒回答「搬走後還擁有什麼」。 | 想知道身分、親緣、位置該不該是同一件事 |
| [不專屬單題的部分](open-threads.md) | 還在生長的想法（能力是什麼、搬移是什麼、C 的最小 ABI、分層 kernel 快照放哪）、八題大家問出來的問題、六個明顯的坑。 | 想知道四題共同還沒解的、以及最容易踩錯的地方 |
