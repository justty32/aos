# 主題：核心行程、子行程，與外部處理器的契約

← [workshop](../../README.md)

| | |
|---|---|
| **開場** | 2026-08-25 |
| **已跑輪數** | R1（各自發想）、R2（延伸／組合／變體） |
| **狀態** | 進行中 |
| **參與身份** | 資深工程師 / 資深架構師 / 資深研究人員（作業系統／體系結構） / 要接這個工具的開發者 |
| **缺哪個角度** | **「普通用戶」開場前被使用者拿掉**——理由是現階段還在規劃，複雜的東西之後會包起來只留簡單介面，那個身份只能評判 CLI 好不好用。所以本紀錄**沒有「這東西人看不看得懂」的視角**，續場若要談介面，要補回來。 |
| **reasoning effort** | `xhigh`（本場使用者指定，非 workshop 預設的 medium） |

**主題**：CPU 只該做「取指令→執行→再取」。那匯聚、排下一回合、跑 funcs 該不該變成
`inst.json` 裡**恆常存在的頭尾核心系統指令**？以及「行程」在 aos 裡到底是什麼——
資料夾是主行程、funcs 是子行程、都由核心 CPU 執行；多核就是多支 `aos exec` 指向不同
`inst.json`；外部處理器則簡單到不必引用 aos lib。

**本場不設範圍外**，明講可以主張推翻現有任何決定。

---

## 這場拆成四份

| 檔案 | 裡面有什麼 | 什麼時候會想看 |
|---|---|---|
| [使用者拍板的三條](decisions.md) | R1 之後使用者當場拍板的三條（`kernel.json` 收、`func` 拆成 job 與 lane、多核學 Linux），以及他同一輪注入的新概念「行程有自己的資料夾」 | **要知道哪些已經是前提、不必再議**；也是 R2 的主軸出處 |
| [R1 想法池](r1.md) | R1（各自發想）的四段：成形的方向、還在生長的想法、大家問出來的問題、明顯的坑。核心系統指令、匯聚歸屬、行程定義、`dofuncs`、多核、零 lib 契約、exe／lib 切線 | 想知道那三條拍板是從哪些理由長出來的，或要找外部處理器契約的清單 |
| [R2 想法池](r2.md) | R2（延伸／組合／變體）的四段：巢狀拓樸 A／B 與「同一磁碟 ABI、兩種執行拓樸」、job→lane 升格、root 的至高權力清單、kernel 分層合成 | 要談子行程佈局、親緣綁路徑還是 UUID、或 job 與 lane 怎麼分工 |
| [轉交提案](handoff.md) | 四條**未拍板**、要使用者決定才能改規格的事 | 要拍板下一步改什麼規格時 |

---

## 續場資訊

四個 codex session **沒有結束、context 留著**，R2 已經用 `codex exec resume <id>` 接續成功，
**R3 用同一批 id**（resume 之後 `thread_id` 不變）。**只在開場那台機器上有效**——office Windows。

> **`codex exec resume` 不吃 `-s` 與 `-C`**（沙盒與工作目錄從原 session 繼承），
> 帶了會直接 usage error 退出碼 2。`--json`／`-o`／`-c` 照常。事件裡的欄位是
> **`thread_id`**，不是 `session_id`。

| 身份 | session id |
|---|---|
| 資深工程師 | `01a03653-61a9-7f21-9615-6c2921f1ead6` |
| 資深架構師 | `01a0364d-b8e1-7113-af65-bdf892bb14ef` |
| 資深研究人員（OS／體系結構） | `01a0364f-6a54-7713-844e-dfd70278cbe0` |
| 要接這個工具的開發者 | `01a03651-d860-74d0-ae5e-22d83cc959d3` |

> 原本是單檔 `core-process-and-subprocess.md`，20 KB，照 [DEV-GUIDE](../../../../STRUCTURE.md) 的「膨脹即拆」按輪次與用途拆成本資料夾。
