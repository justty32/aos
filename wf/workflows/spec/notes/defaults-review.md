# E～M 章 45 條建議預設：照今天的證據還站得住嗎

日期 2026-09-05。只審「使用者還沒答」的 45 條，問一句話：有了原型 73 條撞到的事（`proto/FINDINGS.md`）、83 條邊緣狀況（`spec/notes/edge-cases.json`）、主編的取捨（`edge-decisions.md`）與今天兩批裁決（`rulings-2026-09-05.md`）之後，當初的建議預設還對嗎。

**先講數字**：站得住 24 條、建議改 12 條、已被今天的裁決連帶決定 9 條。

**編號對帳**：交辦單列的是 41 條（E-02～E-06、F-03～F-07、G-02～G-06、H-03～H-05、I-05～I-08、J-02～J-06、K-01～K-06、L-01～L-06、M-02、M-03）。少掉的 4 條是 F-01、H-01、H-02、J-01——它們被 M-01「09-04 那批整批升格」順手裁掉了，所以我把它們放進「連帶已決定」那段，41＋4＝45。

**依據怎麼念**：`F#n`＝FINDINGS 前五條擋路的第 n 條；`邊 Bxx／Sxx／Lxx`＝邊緣狀況那 83 條的編號；`裁 X`＝今天的裁決；`spec 現狀`＝主編已經照證據改寫、只是還掛著〔預設〕標籤。注意有兩套 S 編號：`裁 S-02` 是今天裁決單那八題之一，`邊 S02` 是邊緣狀況第 52 條，別看混。

## 逐條

| 編號 | 問題 | 建議預設 | 結論 | 依據 |
|---|---|---|---|---|
| E-02 | json 塔與 shell 誰是主 | 人寫原稿、載入器編進 `.aos/` 才叫腳本 | 站得住，照預設 | 邊 B40／B41 |
| E-03 | 預算單位與誰執行 | 第一版用步數，run 自己數 | 站得住，照預設 | 邊 L05／B08 |
| E-04 | 源碼註解放哪 | 留 `_comment`，編譯器一律略過 | 建議改：略過但原樣抄進中間表示 | 邊 L06 |
| E-05 | 堆疊框誰丟何時丟 | 推游標同一步刪，壞的留著 | 站得住，照預設 | 邊 B46 |
| E-06 | 型別怎麼拆平 | 簽名只住原稿，編譯後丟掉 | 站得住，照預設 | 邊 L07（放） |
| F-03 | 要不要第三種「有變動才動」 | 不另立，用門鈴代替 | 建議改：加 `--until never`、門鈴收件人改 daemon | 邊 B12、P6 |
| F-04 | 慢指令誰判定 | 靠 PATH 擋，自標脫節後補 | 建議改：登記表 `slow` 自標，第一版就讀 | 邊 S16 |
| F-05 | 半途超預算怎麼記 | 不允許半途，只在原子邊界檢查 | 站得住，照預設 | 邊 B13 |
| F-06 | timeout 預設開不開、多少 | 預設開當保險絲，run 那層給值 | 站得住，照預設（60 s；LLM 世界另有豁免） | 邊 B13／L03 |
| F-07 | 脫節子地的鐘父能不能查 | 能，查 daemon 登記表 | 建議改：LLM 請求不是地，要查請求狀態 | 邊 B03 |
| G-02 | 一次性入口長什麼樣 | `aos daemon exec <地>` 投一筆一次性登記 | 站得住，照預設 | 邊 S04、P9 |
| G-03 | 登記一筆有哪些欄 | 路徑、pid、時鐘規格、狀態 | 站得住，照預設（實際又多了幾欄） | 邊 B07／B43 |
| G-05 | 處理單元位址記在哪 | 使用者層設定檔，世界層只能覆蓋上限 | 站得住，照預設（起 LLM 世界時抄一份給它） | 邊 S06 |
| G-06 | 並行上限由誰數 | daemon 起之前看登記表 | 建議改：LLM 併發歸 LLM 世界自己數 | 邊 S05 |
| H-03 | agent 第一版要哪些限制 | 總時限、LLM 次數、token 上限 | 建議改：總時限改格數、token 對自己的用量檔判 | 邊 L02／B19 |
| H-04 | 每圈 prompt 怎麼組 | 上圈結果＋信＋固定地圖 | 站得住，照預設（信要同格搬出收件匣） | 邊 S07 |
| I-05 | 重複投遞怎麼辦 | 同 id 一律拒絕並回報 | 站得住，照預設（去重表記在接力棒檔頭） | F#3、邊 B34 |
| I-06 | 無效投遞誰通知 | 隔離該檔、其餘照跑、一定回寫錯誤 | 站得住，照預設（回不了就記地上的錯誤日誌） | 邊 B35／S14 |
| I-07 | 環境變數繼承預設值 | 欄位必備，預設繼承但建議關 | 建議改：預設改成不繼承，另訂保底 PATH | FINDINGS 核心段 |
| I-08 | 足跡宣告第一版做不做 | 宣告但不強制，先卡欄位 | 建議改：同格宣告相交要整格拒跑 | 邊 S08 |
| J-02 | 第一級先做哪幾項 | 先偵測出生與死亡，記帳第二 | 站得住，照預設 | 邊 S10／B37 |
| J-03 | 「只記不做」升不升格 | 升格 | 站得住，照預設 | 邊 B12 |
| J-05 | 脫節回寫算不算「別人」 | 算自己人，直接落地 | 建議改：可落地，但父每格只在格頭看一次 | 邊 S09 |
| J-06 | 門房是子命令還是另一支程式 | aos 自己的子命令 | 站得住，照預設 | — |
| K-01 | agent 底下的工具白名單留不留 | 拿掉，只留世界層登記表 | 站得住，照預設 | 邊 B38 |
| K-02 | 迴圈第一版讀登記表哪幾欄 | 一欄都不讀 | 建議改：只讀 `slow`，其餘保留無消費者 | 邊 S16 |
| K-03 | 通訊錄檔案形狀 | 頂層陣列，每列三欄 | 站得住，照預設（放不下版本欄，主編已選照它） | spec 現狀 |
| K-04 | `aos llm` 退出碼分幾種 | 四種（含「可重試」） | 建議改：四種全歸「跑沒跑起來」，可重試寫狀態檔 | 邊 S18、FINDINGS |
| K-05 | 要不要現在就有輸出形狀欄 | 留欄先不用 | 站得住，照預設 | — |
| K-06 | 要不要標「這工具慢」 | 加一欄，登記者自填 | 站得住，照預設（現在真的有人讀了） | 邊 S16 |
| L-01 | 指令名用 `pu` 還是 exec／run | 不用 `pu` | 站得住，照預設 | — |
| L-02 | 核心名冊六支夠不夠 | 六支收，agent 那組歸指令面 | 站得住，照預設（原型缺的都歸指令面） | FINDINGS run 段 |
| L-03 | 要不要第一天就立正本規範 | 先立薄的一份，邊玩邊長 | 建議改：已經是 757 條全套，改寫成「這疊就是正本」 | spec 現狀 |
| L-04 | 版本欄放哪怎麼標 | 各一個檔、各一個欄 | 站得住，照預設（加了拒跑與 `aos migrate`） | 邊 B22／S19 |
| L-05 | 控制面走投遞協定嗎 | 走，只有立刻殺掉走另一條 | 站得住，照預設（讀取點補成三處） | 邊 B48／B49／L10 |
| L-06 | 「同 commit 回寫文件」立不立鐵律 | 立，只約束裁決與規範 | 站得住，照預設 | — |
| F-01 | 09-04 時間模型整批升格嗎 | 升格 | 已被 M-01 決定 | 裁 M-01 |
| H-01 | 「一個 agent＝一個資料夾」升格嗎 | 跟那批一起升格 | 已被 M-01 決定 | 裁 M-01 |
| H-02 | agent 停止條件＝run 通用停法嗎 | 是同一條 | 已被 M-01＋S-01 決定 | 裁 M-01／S-01 |
| H-05 | agent 借父鐘還是自己登記 | 預設借父的鐘 | 已被 S-02 反過來：自己登記 | 裁 S-02 |
| G-04 | 08-30 那套排程機制作不作廢 | 機制作廢、五條規則保留 | 已被 S-05 決定：規則改由 `aos llm serve` 每輪執行 | 裁 S-05 |
| J-01 | FUSE 什麼時候做 | 不排進第一塊 | 已被 M-01 決定（順序 tmpfs→inotify→FUSE） | 裁 M-01 |
| J-04 | `.aos/` 放 tmpfs 的前提 | 只當實驗開關，預設在磁碟 | 已被實作裁決 F-09 決定（同預設） | 裁 F-09 |
| M-02 | 「先玩」的結束條件 | 玩到能裁那六條為止 | 已被今天達成：六條全裁了，新範圍由 P-02 給 | 裁 P-02 |
| M-03 | OS 的第一塊選什麼 | 門房第一級＋成本帳關口 | 已被實作裁決 F-06 決定：第一塊是 M1「一步走得動」 | 裁 F-06 |

## 我會挑出來請使用者改的（12 條）

- **E-04**：`_comment` 不是丟掉，而是**原樣抄進中間表示**——不然出事時看接力棒完全不知道這串在幹嘛（邊 L06）。
- **F-03**：除了門鈴，還得補一種「閒著也不停、掛著等信」的走法（`--until never`），LLM 世界正需要；門鈴的收件人要是 daemon 不是那塊地，否則信落地時 run 已經死了（F 原型 run 段、邊 B12）。
- **F-04**：慢不慢改成**登記工具的人自己標**，而且第一版就讀那一欄——原本說「後補的糖」，但 agent 要靠它決定原地跑還是開脫節子地（邊 S16）。
- **F-07**：「查登記表」只對地成立；**一筆 LLM 請求不是地**，查不到。要給請求一個看得到的狀態（排隊中／送出中／好了／壞了）（邊 B03）。
- **G-06**：並行上限**不再由 daemon 數**——LLM 已經是單獨一個世界，讓它自己數 in-flight；daemon 只保證那支 run 活著。兩邊都數會變成雙重限流或兩邊都不管（邊 S05）。
- **H-03**：三個上限留著，但**總時限改成格數**（父的鐘停三天，agent 一醒來就被牆鐘判死），**token 上限對 agent 自己地上的用量檔判**（token 數只有 LLM 世界知道，不改就等於沒設）（邊 L02、B19）。
- **I-07**：環境變數**預設改成不繼承**——原本「預設繼承、但文件建議關」等於預設就是不建議的那個，金鑰會被帶進每一支子程式；同時要明訂關掉之後的保底 PATH（FINDINGS 核心段）。
- **I-08**：足跡照樣不強制，但**同一格裡兩筆指令宣告要寫同一個檔就整格拒跑**——不然誰後寫誰贏，直接打到「可預測性最優先」（邊 S08）。
- **J-05**：脫節回寫可以隨時落地，但**父每格只在格頭看一次**——不然同一格前後看到兩個世界，「一格＝一筆交易」破功（邊 S09）。
- **K-02**：「第一版一欄都不讀」改成**只讀 `slow` 那一欄**，其餘保留無消費者——不改的話 K-06 那一欄是死的（邊 S16）。
- **K-04**：`aos llm` 四種退出碼保留，但**全歸「跑沒跑起來」**，「該不該重試」寫進狀態檔——不然退出碼跟 I-04 的兩頻道自相矛盾（邊 S18、FINDINGS LLM 段）。
- **L-03**：「先立薄的一份」已經被事實蓋過——**現在就是 757 條 spec＋schemas 當正本**，這條改成描述現況比較誠實（spec 現狀）。

## 連帶已決定、不用再問的（9 條）

- **F-01、H-01、H-02、J-01**：都在 09-04 那批裡，被 **M-01** 整批升格；H-02 的字面又被 **S-01** 改寫成「沒新指令**且**沒有串在等才停」。
- **H-05**：被 **S-02** 反過來——agent 預設自己登記時鐘，借父鐘變成可選。
- **G-04**：機制作廢已成 spec 現狀；那五條規則改由 **S-05** 裁的 `aos llm serve` 每一輪執行。
- **J-04**：被實作裁決 **F-09** 決定——tmpfs 開關第一版就留、預設仍在磁碟，跟原預設一致。
- **M-02**：先玩的結束條件（能裁 E-01、I-01～I-04、G-01）**今天六條全裁完了**，條件已達成；接下來玩什麼由 **P-02** 定：接真的 LM Studio、玩到 agent。
- **M-03**：被實作裁決 **F-06**（八個里程碑照這樣）取代——第一塊是 M1「一步走得動」，門房第一級排到 M7，成本帳散在 M5／M8。
