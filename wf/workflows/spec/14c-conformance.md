# 14c 合規檢查表（10～13）
← [入口](README.md)｜[14 前半與怎麼跑](14-conformance.md)｜[14b 中段](14b-conformance.md)

接 [14](14-conformance.md)。檢查怎麼跑、四種種類怎麼寫都在那一份。最後一段是所有機器測不到的條款。

## 10 agent

| 條款 | 檢查怎麼做 | 種類 | 用什麼 |
|---|---|---|---|
| S-10-01 | agent 資料夾有 `.aos/`，刪掉就沒了 | 存在 | 看目錄 |
| S-10-02 | 用的是同一支 exec | 回傳 | `aos exec <agent>` 跑得動 |
| S-10-03 | 核心沒有 agent 迴圈的子命令 | 回傳 | `aos --help` |
| S-10-04 | 同 S-10-02 | 回傳 | 同上 |
| S-10-05 | 停掉再起，接著上一步走 | 回傳 | 同 S-02-35 |
| S-10-06 | 四樣都在 | 存在 | 看檔 |
| S-10-07 | init 產出的圈直接 run 跑得完 | 回傳 | 退 0 |
| S-10-08 | `agent.json` 過 schema、在頂層、沒被忽略 | schema | agent.schema.json |
| S-10-09 | `system_prompt` 是路徑 | schema | 同上 |
| S-10-10 | 改帳簿不影響、改 `.aos/usage.json` 才影響 | 回傳 | 兩邊各改一次 |
| S-10-11 | 每圈把落點旁的 `.usage.json` 累加進去 | schema | usage-total.schema.json |
| S-10-12 | 超過上限時出現停；借父鐘的由父讀到 | 回傳 | 讀停止原因檔 |
| S-10-13 | 圈自己不看牆上的鐘 | 人工 | 要讀程式碼才看得出來 |
| S-10-14 | 缺席或 0 時跑得比上限久 | 回傳 | 跑一趟 |
| S-10-15 | 模板裡六步的順序 | 回傳 | 讀模板檔 |
| S-10-16 | 組 prompt 那步的輸入只有三個路徑 | 回傳 | 讀落檔的指令 |
| S-10-17 | 進了 prompt 的信搬到 `.aos/mail/read/` | 存在 | 看兩個目錄 |
| S-10-17 | 搬不成功的沒進 prompt | 回傳 | `grep` prompt 檔 |
| S-10-18 | 字串模板留給實作 | 人工 | 是「不規定」 |
| S-10-19 | 每圈第一筆先清舊的結果、狀態、用量三個檔 | 回傳 | 讀模板檔 |
| S-10-20 | 投 LLM 那步跑的是 `aos deliver` | 回傳 | 讀模板檔 |
| S-10-21 | 落點在堆疊框、tier 來自 `agent.json`、工具行來自 `.aos/tools/` | 回傳 | 讀請求 |
| S-10-22 | 下一步是 `await`，等的就是那個落點 | 回傳 | 讀模板檔 |
| S-10-23 | 回話格式不合算失敗，空清單不算 | 回傳 | 讀 `status` |
| S-10-24 | 沒標 slow 的走 `inst`，slow 的走脫節加等 | 回傳 | 讀模板檔 |
| S-10-25 | 工具結果過封套 schema | schema | tool-envelope.schema.json |
| S-10-26 | 同 S-06-18 | 回傳 | 同上 |
| S-10-27 | 停在 `await` 上的串不會讓 run 停 | 回傳 | 同 S-05-50 |
| S-10-28 | 空清單那圈串變 done、run 停 | 回傳 | 讀接力棒 |
| S-10-29 | 停完投一封信再 run，又開一圈 | 回傳 | 讀 `tick` |
| S-10-30 | init 產的父原稿用 `mode:"sync"` | 回傳 | 讀原稿 |
| S-10-31 | 帶 `--register` 起得來 | 回傳 | 讀登記表 |
| S-10-32 | agent 地上不准有等外面的指令 | 人工 | 機器判不出哪支程式會等外面 |
| S-10-33 | 同 S-10-05 | 回傳 | 同上 |
| S-10-34 | 兩種壞法各造一次 | 存在 | 狀態檔 `killed` 與 `result_unknown` |
| S-10-35 | 沒寫 `on_fail` 時串停在原地，沒有重投 | 回傳 | 數落檔的請求筆數 |
| S-10-36 | 等待期間世界變了不算錯 | 人工 | 沒有可觀察的失敗 |
| S-10-37 | 等待期間進來的信進下一份 prompt | 回傳 | `grep` prompt 檔 |
| S-10-38 | agent 地上沒有第二份工具清單 | 存在 | 同 S-02-14 |
| S-10-39 | 家有收件匣，寄得進去 | 存在 | 看檔 |
| S-10-40 | 信有 `from` | schema | delivery.schema.json |
| S-10-41 | 三支子命令各跑一次 | 回傳 | 退 0 且輸出對 |
| S-10-42 | 游標停在不同步時輸出不同 | 回傳 | 比對 `aos state` 的輸出 |
| S-10-43 | 累加是 `await` 之後自己一步，不跟它同格 | 回傳 | 讀模板檔與 `.aos/usage.json` |
| S-10-44 | agent 沒開過家的設定檔 | 回傳 | 同 S-05-60 |
| S-10-45 | 骨架的六種步與順序沒被改 | 回傳 | 同 S-10-15 |
| S-10-46 | 骨架裡沒有 `~` 或寫死的家路徑 | 回傳 | `grep` 骨架原稿 |
| S-10-47 | 三態靠那幾個步名認 | 回傳 | 同 S-10-42 |

## 11 工具與通訊錄

| 條款 | 檢查怎麼做 | 種類 | 用什麼 |
|---|---|---|---|
| S-11-01 | 一支普通程式登記得起來 | 回傳 | `aos tool add` 退 0 |
| S-11-02 | 一個可執行檔一個檔 | 存在 | 看 `.aos/tools/` |
| S-11-03 | 叫沒登記的工具 | 回傳 | 封套帶 `error` |
| S-11-04 | `aos tool add` 之前那個目錄是空的 | 存在 | 看目錄 |
| S-11-05 | 拿 agent 資料夾當工具登記 | 回傳 | 退非 0 |
| S-11-06 | 檔名跟 `name` 一致 | 回傳 | 比對 |
| S-11-07 | agent 底下沒有第二份登記表 | 存在 | 同 S-02-14 |
| S-11-08 | 只有三欄必填 | schema | tool.schema.json |
| S-11-09 | 沒有巢狀分組；怪欄位不過 | schema | 同上 |
| S-11-10 | `list` 時空白與引號原樣傳到 argv | 回傳 | 看工具收到什麼 |
| S-11-11 | 沒有具名槽的欄位 | schema | tool.schema.json |
| S-11-12 | 同 S-10-38 | 存在 | 同上 |
| S-11-13 | 沒開就不試第二種自述旗標 | 回傳 | 假工具數被叫幾次 |
| S-11-14 | 手填蓋掉探到的，`filled_by` 記對 | 回傳 | 讀工具檔 |
| S-11-15 | 沒自述時可預期性缺席 | 回傳 | 讀工具檔 |
| S-11-16 | 工具的狀態沒寫進 `.aos/` | 存在 | 同 S-02-14 |
| S-11-17 | 這兩樣沒被 git 忽略 | 回傳 | 同 S-02-19 |
| S-11-18 | 可預期性怎麼讀 | 人工 | 兩種讀法產出同一個檔 |
| S-11-19 | 三選一，缺席不補成 high | schema | tool.schema.json |
| S-11-20 | 登記一個 LLM 類工具不填可預期性 | 回傳 | 退 0，欄位缺席 |
| S-11-21 | 沒有發證那一層的檔 | 存在 | 同 S-02-14 |
| S-11-22 | 除了 `slow`，改那四欄行為都不變 | 回傳 | 改值前後跑出來一樣 |
| S-11-23 | 之後先讀哪一個 | 人工 | 這一版沒有行為可測 |
| S-11-24 | 同 S-11-22 | 回傳 | 同上 |
| S-11-25 | 沒填 `slow` 時當快的 | 回傳 | 走 `inst` 步 |
| S-11-26 | 同 S-10-24 | 回傳 | 同上 |
| S-11-27 | 那一行有六樣 | 回傳 | 看 `aos tool ls` |
| S-11-28 | 那一行照格式，缺可預期性寫 unknown | 回傳 | 正規式對輸出 |
| S-11-29 | 描述要寫出代價 | 人工 | 機器讀不出一句話有沒有寫到 |
| S-11-30 | 那一行裡沒有說明檔的內容 | 回傳 | 比長度 |
| S-11-31 | 匯出的物件裡沒有非標準欄 | 人工 | 這一版沒有匯出指令可打 |
| S-11-32 | 同 S-10-23 | 回傳 | 同上 |
| S-11-33 | 成功與失敗同一形狀 | schema | tool-envelope.schema.json |
| S-11-34 | 六個欄 | schema | 同上 |
| S-11-35 | 工具不存在或型別錯，用封套退回 | 回傳 | 讀封套 |
| S-11-36 | 截斷時 `truncated` 是 true | 回傳 | 讀封套 |
| S-11-37 | `call_id` 串得回去，封套沒被清 | 存在 | 看檔還在 |
| S-11-38 | 一次快工具往返剛好三格 | 回傳 | 比對 `tick` |
| S-11-39 | slow 的不算進那三格 | 回傳 | 同上 |
| S-11-40 | 把 `sh` 登記起來，模型叫得動 | 回傳 | 封套 `ok` 是 true |
| S-11-41 | 白名單不宣稱安全 | 人工 | 是文字，不是行為 |
| S-11-42 | 通訊錄過 schema | schema | contacts.schema.json |
| S-11-43 | init 與開子地都不動它 | 存在 | 前後 `sha256sum` |
| S-11-44 | 檔裡沒有 `~` 那列，列出來卻有 | 回傳 | 比對 |
| S-11-45 | 路徑不在時照樣列得出來 | 回傳 | 退 0 |
| S-11-46 | 寄信是一支登記過的工具 | 存在 | 看 `.aos/tools/` |
| S-11-47 | 同 S-09-36 | schema | 同上 |
| S-11-48 | 子 agent 的設定不從父帶 | 回傳 | 讀子的 `agent.json` |
| S-11-49 | 第 0 個引數叫不動就不登記 | 回傳 | 退非 0、沒有新檔 |
| S-11-50 | 列表、刪除、遇到壞檔整次報錯 | 回傳 | 退非 0 |
| S-11-51 | 三支各跑一次，同名取代，`~` 排最前 | 回傳 | 比對輸出 |
| S-11-52 | 寫檔走暫存再改名 | 回傳 | 同 S-05-40 |
| S-11-53 | 登記 `aos llm` 當工具 | 回傳 | 同 S-09-58 |
| S-11-54 | 寄到一個沒有 `.aos/` 的資料夾 | 回傳 | 封套 `no_such_land`，沒有新目錄 |
| S-11-55 | 那則 `error.message` 提到是哪一列哪個路徑 | 回傳 | `grep` 封套 |
| S-11-56 | 工具跑起來的 `pwd` 是呼叫它那塊地的根 | 回傳 | 看 stdout |
| S-11-57 | `cwd` 寫絕對路徑或 `..` | 回傳 | `aos tool add` 與 `aos check` 都退非 0 |
| S-11-58 | `error.kind` 是短代碼 | schema | tool-envelope.schema.json |
| S-11-59 | `error.message` 放 `$(id)` | 回傳 | 下一筆指令裡沒有被展開 |

## 12 指令面

| 條款 | 檢查怎麼做 | 種類 | 用什麼 |
|---|---|---|---|
| S-12-01 | 裝出來只有一支執行檔 | 存在 | 看安裝清單 |
| S-12-02 | 七個退出碼各造一次 | 回傳 | 同 S-06-24 |
| S-12-03 | `--interface` 把兩條流轉接出去 | 回傳 | 假程式收得到 |
| S-12-04 | `run --steps` 與 `status` 都在 | 回傳 | `--help` |
| S-12-05 | 每支子命令 `--help` 退 0 | 回傳 | 逐支跑 |
| S-12-06 | 同 S-06-01 | 回傳 | 同上 |
| S-12-07 | 同 S-06-16 | 回傳 | 同上 |
| S-12-08 | 同 S-08-06 | 回傳 | 同上 |
| S-12-09 | 跑一格時那支行程沒有對外連線 | 回傳 | 看 `/proc/<pid>/net` |
| S-12-10 | 其他子命令不准碰 LLM | 人工 | 同 S-04-27 |
| S-12-11 | init 後三樣都在 | 存在 | 看檔 |
| S-12-12 | init 後沒有接力棒 | 存在 | 看檔 |
| S-12-13 | `aos pu` 不存在 | 回傳 | 退 2 |
| S-12-14 | 同 S-02-18 | 回傳 | 同上 |
| S-12-15 | reset 清 failed 與 stopped，`--all` 整份刪 | 存在 | 讀接力棒 |
| S-12-16 | reset 前後頂層檔沒變 | 存在 | 比對清單 |
| S-12-17 | 有 run 在跑時 reset | 回傳 | 退 75 |
| S-12-18 | 輸出同時有登記表與游標 | 回傳 | `grep` 輸出 |
| S-12-19 | `aos stop` 之後控制收件匣有一則 | 存在 | 看目錄 |
| S-12-20 | `--kill` 不經過控制收件匣 | 存在 | 同 S-08-60 |
| S-12-22 | 同 S-02-10 | 回傳 | 同上 |
| S-12-23 | 同 S-08-50 | 回傳 | 同上 |
| S-12-24 | 六支都在 | 回傳 | `aos --help` |
| S-12-25 | 加第七支的判準 | 人工 | 判準是給人用的 |
| S-12-26 | 那五支在 `aos agent` 底下 | 回傳 | `aos --help` |
| S-12-27 | 兩支單獨跑得動 | 回傳 | 同 S-10-41 |
| S-12-28 | 核心切四圈 | 人工 | 只能讀程式碼 |
| S-12-29 | 同 S-04-13 | 人工 | 同上 |
| S-12-30 | 六支與四圈是兩份表 | 人工 | 是文件的說法 |
| S-12-31 | 每種 json 都有一份 schema | 存在 | 比對 `schemas/` 與 02 的表 |
| S-12-32 | 三樣各造一個壞檔 | 回傳 | `aos check` 都報得出來 |
| S-12-33 | `aos check` 跑完檔案沒變 | 回傳 | 前後 `sha256sum` |
| S-12-34 | 同 S-02-21 | schema | 同上 |
| S-12-35 | 同 S-02-22 | schema | 同上 |
| S-12-36 | 兩個版本欄都是整數 | schema | layout.schema.json |
| S-12-37 | 同 S-02-38 | 回傳 | 同上 |
| S-12-38 | 同 S-02-13 | 人工 | 同上 |
| S-12-39 | 改裁決卻沒改 spec 的 commit | 人工 | 沒有綁 commit 與條款的東西 |
| S-12-40 | 同 S-12-39 | 人工 | 同上 |
| S-12-41 | 方向性的裁決屬於使用者 | 人工 | 是流程規定 |
| S-12-42 | 每條句尾有三種來源標記之一 | 回傳 | 正規式掃 `spec/*.md` |
| S-12-43 | 判例記錄寫了為什麼 | 人工 | 機器判不出理由夠不夠 |
| S-12-44 | 同 S-06-38 | 回傳 | 同上 |
| S-12-45 | 指令跑一半投停，最壞等那筆跑完 | 回傳 | 量時間差 |
| S-12-46 | `--kill` 之後有狀態檔、現場還在 | 存在 | 同 S-08-78 |
| S-12-47 | `aos fix` 把現場改名、串標 stopped | 存在 | 看 `.aos/ticks/<N>.crashed/` |
| S-12-48 | `aos fix --requeue` 把投遞物退回收件匣 | 存在 | 看收件匣 |
| S-12-49 | `aos migrate` 只印一行 | 回傳 | 同 S-02-39 |
| S-12-50 | 除了兩支，其他子命令都拒跑 | 回傳 | 逐支跑，都退 3 |
| S-12-51 | 同 S-09-65 | 回傳 | 同上 |
| S-12-53 | 同 S-06-34 與 S-06-35 | 回傳 | 同上 |
| S-12-54 | 同 S-05-47 | 回傳 | 同上 |
| S-12-55 | 同 S-08-67 | 回傳 | 同上 |
| S-12-56 | `aos llm serve` 的預設與鎖 | 回傳 | 同 S-09-72、S-09-73 |
| S-12-57 | `--until` 收 idle 與 never | 回傳 | `aos run --help` |
| S-12-58 | 同 S-08-86 | 回傳 | 同上 |
| S-12-59 | `aos llm ask` 兩個檔都留著 | 存在 | 看 `$AOS_HOME/.aos/ask/` |
| S-12-60 | `aos publish` 兩種寫法 | 回傳 | 同 S-07-66 |
| S-12-61 | 沒人在跑時 `aos stop` | 回傳 | 退 0，控制收件匣是空的 |
| S-12-62 | 處理過的控制信搬到 `done/` | 存在 | 同 S-06-55 |
| S-12-63 | 登記表沒 pid 時改讀鎖檔 | 回傳 | 同 S-08-93 |
| S-12-64 | `aos status` 三樣都印 | 回傳 | `grep` 輸出 |
| S-12-65 | `aos status --triple` 三態各跑一次 | 回傳 | 比對輸出 |
| S-12-66 | `aos deliver` 拒收時說是哪個欄位 | 回傳 | `grep` stderr |
| S-12-67 | `migrate`／`publish`／`daemon add`／`llm serve`／`llm ask` 歸指令面、不算第七支 | 人工 | 文件說法；12b 名冊表不含它們即可 |

## 13 門房第一級

| 條款 | 檢查怎麼做 | 種類 | 用什麼 |
|---|---|---|---|
| S-13-01 | `aos doorman` 是子命令 | 回傳 | `aos --help` |
| S-13-02 | 沒有第二種腳本，地裡沒有門房設定 | 存在 | 同 S-02-14 |
| S-13-03 | 門房底下是真目錄 | 回傳 | `stat -f` 不是接管出來的 |
| S-13-04 | 殺掉門房，exec 照跑 | 回傳 | 退 0 |
| S-13-05 | 用的是 inotify | 回傳 | `ls /proc/<pid>/fd` |
| S-13-06 | 往下走的順序 | 人工 | 是排程，不是行為 |
| S-13-08 | 收到事件後只有本子與登記表變 | 回傳 | 別的檔 `sha256sum` 不變 |
| S-13-09 | 門房跑著時地的 `tick` 不動 | 回傳 | 讀接力棒 |
| S-13-10 | 同 S-13-09 | 回傳 | 同上 |
| S-13-11 | 本子在，每行都是 json | 回傳 | 逐行丟 `jq` |
| S-13-12 | 每行過 schema | schema | doorman-log.schema.json |
| S-13-13 | `event` 六選一 | schema | 同上 |
| S-13-14 | 生死先做、記帳後做 | 人工 | 是做事順序，不是成品行為 |
| S-13-16 | 刪掉登記過的地 | 存在 | 本子多一行 `gone` |
| S-13-17 | 路徑沒了但 pid 活著 | 回傳 | 同 S-08-36 |
| S-13-18 | 那筆變 stopped 或被刪 | 回傳 | 讀登記表 |
| S-13-19 | 關掉門房，對帳照樣清 | 回傳 | 同 S-08-37 |
| S-13-20 | 搬走一塊地 | 存在 | 本子有 `gone` 加 `born` |
| S-13-21 | 新出現一塊地 | 存在 | 本子多一行 `born` |
| S-13-22 | 那筆是 stopped，三個欄都 null | 回傳 | 讀登記表 |
| S-13-23 | 出生之後沒有 run 被起 | 回傳 | `ps` |
| S-13-24 | 寫檔事件記進本子 | 存在 | `grep` 本子 |
| S-13-25 | 記帳前後地的行為一樣 | 回傳 | 同 S-13-09 |
| S-13-28 | 一格內十個事件只換來一次處理 | 回傳 | 數登記表的變動次數 |
| S-13-30 | 監看的是登記表的路徑加家底下一層 | 回傳 | 深處的新地不進本子 |
| S-13-31 | 同 S-13-30 | 回傳 | 同上 |
| S-13-32 | 把監看上限調到 1 | 存在 | 本子多一行 `fallback` |
| S-13-33 | 退回巡邏後生死照樣進本子 | 回傳 | 同上 |
| S-13-34 | 門房是獨立行程 | 回傳 | `ps` 看兩個 pid |
| S-13-35 | `aos daemon start --doorman` | 回傳 | `ps` |
| S-13-36 | 同 S-13-19 | 回傳 | 同上 |
| S-13-37 | 兩邊都經過 `registry.lock` | 回傳 | 同 S-08-21 |
| S-13-38 | 預設不放 tmpfs | 回傳 | `stat -f` 看檔案系統 |
| S-13-39 | `aos init --tmpfs` 記在版面檔的 `ext` | 回傳 | 讀檔 |
| S-13-40 | 刪掉 `.aos/program/` 再 exec，重建得回來 | 回傳 | 看檔 |
| S-13-41 | 機器掛了那塊地從頭跑 | 人工 | 要真的斷電或重開機 |
| S-13-42 | 頂層原稿不在 tmpfs | 回傳 | 同 S-13-38 |
| S-13-43 | 第一級不擋寫入 | 回傳 | 寫入的時間差在誤差內 |
| S-13-44 | 將來凍結時結果落點算自己人 | 人工 | 這一版沒有凍結可測 |
| S-13-45 | 其餘寫入等那格結束 | 人工 | 同上 |
| S-13-46 | 新實作的第一塊做什麼 | 人工 | 是排程，不是行為 |
| S-13-47 | 只有 `layout.json` 改名就位才算出生 | 回傳 | 直接複製過去不算，本子沒有 `born` |
| S-13-48 | 只 `mkdir .aos` | 存在 | 本子沒有 `born` |
| S-13-49 | `layout.json` 壞掉 | 存在 | 本子一行 `born_invalid`、登記表沒新筆 |
| S-13-50 | 門鈴投給 daemon | 回傳 | 同 S-08-62 |
| S-13-51 | daemon 自己輪詢本子 | 回傳 | 門房不去戳 daemon |
| S-13-52 | 本子有 `inbox` 而那筆不是 running | 回傳 | daemon 起一支 run |
| S-13-53 | 重開機後從原稿重建 | 人工 | 要真的斷電或重開機 |
| S-13-54 | 那四樣不在 tmpfs 上 | 回傳 | 同 S-02-46 |

## 測不到的必須

57 條只能人工看，缺什麼才測得到寫在後面。另外 S-02-03 的後半「人不准手改 `.aos/`」也測不到（缺防護機制），前半有檢查。

- **S-02-04** 檔案＝名字：缺一個看得出來的後果。
- **S-02-06** 一塊地看不到外面：缺掛載或沙箱，現在沒有東西擋跨地讀寫。
- **S-02-07** 可見性不是權限：缺一個「照可見性擋」的介面來證明它不存在。
- **S-02-13** 一路徑一 writer：缺一支掃程式碼的工具。
- **S-02-15** 記憶的判準：缺一條機器判得出的界線。
- **S-02-36** OS 只保證三件事：缺對應的介面。
- **S-02-37** 摘要交給應用：同上。
- **S-02-45** tmpfs 重建當從頭跑：要真的斷電或重開機。
- **S-04-01** 不再有更小的一格：缺一個違反它的樣子。
- **S-04-13** 逾時做在 run 那層：缺程式碼分層的檢查。
- **S-04-20** 不另做綁定層：缺一個「多了一層」的可觀察後果。
- **S-04-26** 三樣不能寫成指令：缺可以打的介面。
- **S-04-27** LLM 不准當指令：缺一條規定說誰檢查、什麼時候檢查。
- **S-05-17** `resources` 只當參考：缺一個排程行為的差別。
- **S-05-42** 外面不准直接改接力棒：缺擋外部寫的機制。
- **S-05-48** tmpfs 上去重不算保證：同 S-02-45。
- **S-06-05** 十四步的順序：缺程式碼裡的追蹤點。
- **S-06-32** 同步子樹不准有 LLM：同 S-04-27。
- **S-07-05** 同上：同 S-04-27。
- **S-07-08** 子地只看得到父放的：同 S-02-06。
- **S-07-64** 交出落點就是授權：沒有第二種授權可以比對。
- **S-07-65** 子只准寫那三個檔：缺擋寫的機制，這一版寫得進去。
- **S-07-74** `message` 是不可信資料：是讀法，不是行為。
- **S-08-09** 代價明寫：缺一個檢查文件寫了沒的辦法。
- **S-08-44** daemon 不數 LLM 併發：缺程式碼檢查。
- **S-08-49** 理由寫進文件：同 S-08-09。
- **S-08-53** 文件寫成唯一搬法：同上。
- **S-09-05** 不准把 LLM 當指令：同 S-04-27。
- **S-09-06** 同步子樹不准有請求：同上。
- **S-09-35** 只寫變數名：機器分不出字串是變數名還是金鑰，缺「必須對得上一個存在的變數」這條規定。
- **S-10-13** 圈自己不看牆上的鐘：缺程式碼檢查。
- **S-10-18** 字串模板留給實作：是「不規定」，沒有東西可測。
- **S-10-32** 不准跑等外面的指令：缺一份「哪些程式會等外面」的名單。
- **S-10-36** 等待期間世界會變：缺一個違反它的樣子。
- **S-11-18** 可預期性怎麼讀：兩種讀法產出同一個檔。
- **S-11-23** 之後先讀哪一個：這一版沒有行為。
- **S-11-29** 描述要寫代價：缺一份判斷「有沒有寫到代價」的規則。
- **S-11-31** 匯出是投影：這一版沒有匯出指令。
- **S-11-41** 白名單不宣稱安全：是文字。
- **S-12-10** 其他子命令不准碰 LLM：同 S-04-27。
- **S-12-25** 加第七支的判準：判準是給人用的。
- **S-12-28** 核心切四圈：缺程式碼檢查。
- **S-12-29** 逾時在迴圈那圈剝掉：同 S-04-13。
- **S-12-30** 六支與四圈兩份表：是文件的說法。
- **S-12-38** 一路徑一 writer：同 S-02-13。
- **S-12-67** 那五支歸指令面、不算第七支：是文件的說法，只能看 12b 名冊表列了哪幾支。
- **S-12-39** 同 commit 回寫規範：缺把 commit 跟條款綁起來的東西。
- **S-12-40** 新裁決要回寫：同上。
- **S-12-41** 方向性裁決屬於使用者：是流程規定。
- **S-12-43** 理由寫進判例記錄：缺判斷理由夠不夠的規則。
- **S-13-06** tmpfs → inotify → FUSE：是排程。
- **S-13-14** 生死先做記帳後做：是做事順序。
- **S-13-41** 機器掛了從頭跑：要真的斷電或重開機。
- **S-13-44** 凍結時結果落點算自己人：這一版沒有凍結。
- **S-13-45** 其餘寫入等那格結束：同上。
- **S-13-46** 第一塊先做什麼：是排程。
- **S-13-53** 重開機後當從頭跑：同 S-13-41。

回 [14](14-conformance.md) 看「待使用者拍板」與「現況對照」。
