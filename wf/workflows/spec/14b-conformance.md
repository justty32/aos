# 14b 合規檢查表（07～09）
← [入口](README.md)｜[14 前半與怎麼跑](14-conformance.md)｜[14c 後半](14c-conformance.md)

檢查怎麼跑、四種種類怎麼寫都在 [14](14-conformance.md)。

## 07 呼叫、落點與投遞

| 條款 | 檢查怎麼做 | 種類 | 用什麼 |
|---|---|---|---|
| S-07-01 | `mode` 兩選一 | schema | program.schema.json |
| S-07-02 | sync 時父每格對子做一次 exec | 回傳 | 子的 `tick` 跟著加 |
| S-07-03 | 子沒閒著時父的游標不動 | 回傳 | 讀父的接力棒 |
| S-07-04 | 子閒著後父照三態推游標 | 回傳 | 同上 |
| S-07-05 | 同步子樹裡不准有 LLM | 人工 | 同 S-04-27 |
| S-07-06 | async 那步登記完就成功 | 回傳 | 讀 `cursor` 與登記表 |
| S-07-07 | `call` 步不等結果 | 回傳 | 同 S-06-33 |
| S-07-08 | 子地只看得到父放的東西 | 人工 | 同 S-02-06，沒有隔離機制 |
| S-07-09 | 兩種 mode 都寫一筆呼叫記錄 | 存在 | 看 `.aos/calls/` |
| S-07-09 | 呼叫記錄過 schema | schema | call.schema.json |
| S-07-10 | 用 symlink 開子地，記錄裡是真實路徑 | 回傳 | 讀記錄 |
| S-07-11 | `result` 由父寫，子改不了 | 回傳 | 讀記錄 |
| S-07-12 | 子印得出 `AOS_RESULT` 與 `AOS_CALLER` | 回傳 | 同 S-04-37 |
| S-07-13 | `args` 變成 `AOS_ARG_*` | 回傳 | 同上 |
| S-07-14 | 三態各跑一次 | 回傳 | 同 S-05-28 |
| S-07-15 | 狀態檔就在結果落點旁邊 | 存在 | 看檔名 |
| S-07-16 | 狀態檔的欄位 | schema | status.schema.json |
| S-07-17 | `reason` 是小寫短代碼 | schema | 同上 |
| S-07-18 | 三種寫者各造一次 | 存在 | 狀態檔都出現 |
| S-07-19 | 同 S-07-66 | 回傳 | 同上 |
| S-07-20 | 兩個檔都在時算壞了 | 回傳 | 讀 `status` |
| S-07-21 | 同 S-05-25 | 回傳 | 同上 |
| S-07-22 | 同 S-05-28 | 回傳 | 同上 |
| S-07-23 | 同 S-05-31 | 回傳 | 同上 |
| S-07-24 | 失敗後沒有第二次執行 | 回傳 | 數那筆指令的結果檔數 |
| S-07-25 | 同 S-05-32 | 存在 | 同上 |
| S-07-26 | 一條串 failed，另一條照跑 | 回傳 | 讀兩個 `cursor` |
| S-07-27 | 五個動作的先後 | 回傳 | `opened_at` 早於子的第一格 |
| S-07-28 | 清掉 `daemon_pid` 再做脫節呼叫 | 存在 | 落點旁狀態檔 `no_daemon` |
| S-07-29 | 沒有第二個查鐘的辦法 | 回傳 | `aos status` 讀的是登記表 |
| S-07-30 | 帶 `cleanup: true`，`await` 之後子地不見了 | 存在 | 看目錄 |
| S-07-31 | 沒帶時子地還在 | 存在 | 同上 |
| S-07-32 | 巡邏只刪子地，父地的檔都還在 | 存在 | 看兩處 |
| S-07-33 | 投遞是寫暫存再改名 | 回傳 | 同 S-05-40 |
| S-07-34 | 檔還在收件匣時指令不跑 | 存在 | insts 目錄是空的 |
| S-07-35 | `aos deliver` 產的 id 是 32 hex | 回傳 | 看檔名，schema 沒卡 |
| S-07-36 | 互斥靠 `.aos/lock` 不靠狀況檔 | 回傳 | 同 S-05-41 |
| S-07-37 | 留下 `.json.temp` 再跑，它還在 | 存在 | 看目錄 |
| S-07-38 | 投遞物必填五欄 | schema | delivery.schema.json |
| S-07-39 | 同 id 再投 | 回傳 | `aos deliver` 退非 0 |
| S-07-40 | 一格跑到一半投進去的，那格不動它 | 存在 | 看 insts 目錄 |
| S-07-41 | 壞的投遞物進隔離區，別的照跑 | 存在 | 看 `rejected/` |
| S-07-42 | 投遞者收到 `subject` 是 `rejected` 的信 | 存在 | 看 `from` 的收件匣 |
| S-07-43 | `from` 讀不出來時 `.aos/errors.log` 多一行 | 存在 | 看檔 |
| S-07-44 | 塞滿收件匣再投 | 回傳 | `aos deliver` 退 75 |
| S-07-45 | 信搬到 `.aos/mail/`，沒有指令被跑 | 存在 | 看兩個目錄 |
| S-07-46 | 同 S-10-17 | 存在 | 同上 |
| S-07-47 | 子印得出 `AOS_CALL_CHAIN` | 回傳 | 看 stdout |
| S-07-48 | 讓地呼叫自己 | 存在 | 狀態檔 `call_cycle` |
| S-07-49 | 疊到第 9 層 | 存在 | 狀態檔 `call_depth` |
| S-07-50 | 同 S-03-40 | 存在 | 同上 |
| S-07-52 | 同 S-05-47 | 回傳 | 同上 |
| S-07-53 | 投到不是一塊地的資料夾 | 回傳 | 退 4，且沒有新目錄 |
| S-07-54 | 相對落點以父的根為原點 | 回傳 | 讀呼叫記錄 |
| S-07-55 | 記錄裡存的是絕對真實路徑 | 回傳 | 讀呼叫記錄 |
| S-07-56 | 子照 `AOS_RESULT` 原樣寫 | 回傳 | 看檔落在哪 |
| S-07-57 | 落點寫在父地外面 | 回傳 | 退 3 |
| S-07-58 | 落點寫進某個 `.aos/` | 回傳 | 退 3 |
| S-07-59 | 落點以那兩個後綴結尾 | 回傳 | 退 3 |
| S-07-60 | 三個檔都不在才開得成 | 存在 | 看檔 |
| S-07-61 | 先放一個同名檔再開 | 回傳 | 退 3，舊檔沒被動 |
| S-07-62 | 清掉舊檔或換落點就跑得起來 | 回傳 | 退 0 |
| S-07-63 | 同一格兩個落點重複 | 回傳 | 整格退 3 |
| S-07-64 | 交出落點就是授權 | 人工 | 沒有另一種授權可以比 |
| S-07-65 | 子往父地別處寫 | 人工 | 這一版沒有東西擋 |
| S-07-66 | 暫存開在落點同一層，再改名 | 回傳 | `strace -e openat,rename` |
| S-07-67 | 沒有跨目錄暫存、沒有用複製 | 回傳 | 同上 |
| S-07-68 | 半成品不用正式名 | 存在 | 殺掉指令後只看得到暫存名 |
| S-07-69 | 落點放一份壞 json | 回傳 | 那步失敗，只讀一次 |
| S-07-70 | 先刪掉父的 `.aos/`，子放棄寫 | 存在 | 沒有新目錄 |
| S-07-71 | 那筆的 `ext` 有 `result_orphaned` | 回傳 | 讀登記表 |
| S-07-72 | 成功時落點旁有 `.usage.json` | 存在 | 看檔 |
| S-07-73 | 超長 `message` | 回傳 | 被截斷，`ext.truncated` 是 true |
| S-07-74 | `message` 當不可信資料 | 人工 | 是讀法，不是行為 |
| S-07-75 | `message` 放 `$(id)` | 回傳 | 落檔的指令裡沒有被展開 |
| S-07-76 | 同 S-09-57 | 回傳 | 同上 |
| S-07-77 | 同 S-05-61 | 回傳 | 同上，父那邊代寫 `no_result` |
| S-07-78 | `call` 步由 exec 查、`--llm` 由 deliver 查 | 回傳 | 兩支都退 3，那筆不投 |

## 08 daemon 與登記表

| 條款 | 檢查怎麼做 | 種類 | 用什麼 |
|---|---|---|---|
| S-08-01 | 家的 `.aos/` 有登記表、設定、帳簿 | 存在 | 看檔 |
| S-08-02 | 起得來，而且裝出來沒有 systemd 檔 | 存在 | 看安裝清單 |
| S-08-03 | 沒人叫的地不會動 | 存在 | 同 S-02-30 |
| S-08-04 | 起第二支 daemon | 回傳 | 退非 0 |
| S-08-05 | `daemon.pid` 與 `daemon_pid` 同號，停後是 null | 回傳 | 讀兩個檔 |
| S-08-06 | 每塊登記的地各一支 run 子行程 | 回傳 | `ps --ppid` |
| S-08-07 | 四種時鐘各換出對的旗標 | 回傳 | `ps` 看命令列 |
| S-08-08 | 殺掉 daemon，子行程還在 | 回傳 | `ps` |
| S-08-09 | 代價寫進文件 | 人工 | 是文字，不是行為 |
| S-08-10 | 殺掉父的 run，子的還在 | 回傳 | `ps` |
| S-08-11 | 同一個 `path` 只出現一次 | 回傳 | 數 `entries`，schema 沒卡 |
| S-08-12 | 只有一份登記表 | 存在 | 同 S-02-14 |
| S-08-13 | 登記表過 schema | schema | registry.schema.json |
| S-08-14 | 同 S-02-12 | 回傳 | 同上 |
| S-08-15 | 起鐘前 `pid` 是 null，起了填那支 run | 回傳 | 讀登記表 |
| S-08-16 | `state` 三選一 | schema | registry.schema.json |
| S-08-17 | `clock` 四選一，沒鐘的是 null | schema | 同上 |
| S-08-18 | `budget` 是整數或 null | schema | 同上 |
| S-08-19 | `parent` 是真實路徑或 null | schema | 同上 |
| S-08-20 | 兩個時間戳格式，改狀態後 `updated_at` 變新 | 回傳 | 前後比對 |
| S-08-21 | 兩支同時改，兩筆都在 | 回傳 | 讀登記表 |
| S-08-22 | 寫登記表走原子改名 | 回傳 | 同 S-05-40 |
| S-08-23 | 登記表被 git 忽略 | 回傳 | `git check-ignore` 回 0 |
| S-08-24 | run 起來後填那三個欄 | 回傳 | 讀登記表 |
| S-08-24 | 登記表沒那筆時 run 自己補一筆 | 回傳 | 同上 |
| S-08-25 | run 結束後 stopped，兩個 pid 欄都 null | 回傳 | 讀登記表 |
| S-08-26 | `pending` 只有那兩個來源 | 回傳 | 讀登記表 |
| S-08-27 | 門房登記出來的是 stopped、`clock` null | 回傳 | 讀登記表 |
| S-08-28 | 同 S-07-28 | 存在 | 同上 |
| S-08-29 | `aos stop` 之後那筆還在、地也還在 | 存在 | 看目錄與登記表 |
| S-08-30 | 同 S-07-29 | 回傳 | 同上 |
| S-08-32 | 手塞一筆死 pid，起 daemon 後已處理 | 回傳 | 讀登記表 |
| S-08-33 | 行程與路徑兩樣都看 | 回傳 | 下面四條各造一次 |
| S-08-34 | 行程活、路徑在：留 running，不重起 | 回傳 | `ps` 數行程 |
| S-08-35 | 行程死、路徑在：兩種重起、兩種標 stopped | 回傳 | 讀登記表 |
| S-08-36 | 路徑不在、行程活：先停行程再刪那筆 | 回傳 | `ps` 與登記表 |
| S-08-37 | 兩個都不在：直接刪那筆 | 回傳 | 讀登記表 |
| S-08-38 | 對帳完 pending 都變 running | 回傳 | 讀登記表 |
| S-08-39 | 子行程結束後那筆變 stopped | 回傳 | 讀登記表 |
| S-08-40 | 沒有任何通知也清得掉 | 回傳 | 同 S-08-37 |
| S-08-41 | 閒著超過期限，那塊地與那筆都不見了 | 存在 | 看目錄與登記表 |
| S-08-43 | `max_parallel` 設 1，第二塊地留 pending | 回傳 | 讀登記表 |
| S-08-44 | daemon 不數 LLM 併發 | 人工 | 只能讀程式碼 |
| S-08-45 | `aos daemon stop` 走控制收件匣 | 回傳 | `reason` 是 `stop_requested` |
| S-08-46 | 裝一支不理停的假 run，10 秒後整組被殺 | 回傳 | `ps` 看子孫也沒了 |
| S-08-47 | daemon 不在時 stop 也清得乾淨 | 回傳 | 讀登記表 |
| S-08-48 | 補的狀態檔落在 `ext.result` 指的地方 | 存在 | `reason` 是 `killed` |
| S-08-49 | 理由寫進文件 | 人工 | 是文字，不是行為 |
| S-08-50 | `aos daemon exec` 投出一筆 once 的 pending | 回傳 | 讀登記表 |
| S-08-51 | `aos daemon ls` 的行數等於 `entries` 長度 | 回傳 | 比對 |
| S-08-52 | 同 S-02-10 | 回傳 | 同上 |
| S-08-53 | 文件寫成唯一正確的搬法 | 人工 | 是文字，不是行為 |
| S-08-54 | 直接 `mv` 不報錯，對帳清舊筆 | 回傳 | 同 S-02-11 |
| S-08-55 | 沒有搬家通知，symlink 不是第二身分 | 回傳 | 同 S-02-12 |
| S-08-56 | 控制訊息寫暫存再改名 | 回傳 | 同 S-05-40 |
| S-08-57 | 控制訊息過 schema | schema | control.schema.json |
| S-08-58 | 同 S-06-21 | 回傳 | 同上 |
| S-08-59 | 停完訊息不見、停止原因檔在、那筆 stopped | 存在 | 看檔與登記表 |
| S-08-60 | `aos stop --kill` 時控制收件匣一直是空的 | 存在 | 看目錄 |
| S-08-61 | daemon 不在時新的脫節工作起不來 | 存在 | 同 S-07-28 |
| S-08-62 | 門鈴投給 daemon，那塊地沒被直接推 | 回傳 | 地的 `tick` 沒動 |
| S-08-63 | 沒 running 登記時起 `--until idle` | 回傳 | `ps` 看命令列 |
| S-08-64 | `pid_start` 跟 `/proc` 對得上 | 回傳 | 比對 |
| S-08-65 | `land_id` 跟那塊地的一樣 | 回傳 | 比對 |
| S-08-66 | 三個約定的鍵都在 `ext` 裡 | 回傳 | 讀登記表 |
| S-08-67 | `aos daemon exec` 同 id 再投 | 回傳 | 退非 0 |
| S-08-68 | 只改 `pid_start` 就被判成死的 | 回傳 | 讀登記表 |
| S-08-69 | 領養過的登記也判得出死 | 回傳 | 讀登記表 |
| S-08-70 | pending 沒 pid 的重啟後重起 | 回傳 | 讀登記表 |
| S-08-71 | daemon 不改 `path` | 回傳 | 同 S-02-41 |
| S-08-72 | start 印出接管幾個、清掉幾筆 | 回傳 | `grep` 輸出 |
| S-08-73 | `every` 的 run 正常退出後留 stopped 並會重起 | 回傳 | 讀登記表 |
| S-08-74 | `once` 跑完刪那筆，其餘標 stopped | 回傳 | 讀登記表 |
| S-08-75 | 巡邏後父地那三種檔都還在 | 存在 | 看檔 |
| S-08-76 | 刪之前落點沒結果就代寫 | 存在 | 狀態檔 `reaped` |
| S-08-77 | 同 S-06-39 | 回傳 | 同上 |
| S-08-78 | `--kill` 之後也有代寫 | 存在 | 同 S-08-48 |
| S-08-79 | 被殺那格的目錄原封還在 | 存在 | 看目錄 |
| S-08-80 | `aos mv` 後接力棒與游標還在 | 回傳 | 同 S-02-43 |
| S-08-81 | `ext.moved_from` 是舊路徑 | 回傳 | 同上 |
| S-08-82 | 只有 `aos mv` 改得了 `path` | 回傳 | 同 S-08-71 |
| S-08-83 | `runner` 是 run 或 llm-serve，沒寫當 run | schema | registry.schema.json |
| S-08-84 | `llm-serve` 那筆起的是 `aos llm serve` | 回傳 | `ps` 看命令列 |
| S-08-85 | `resume` 是可選布林 | schema | registry.schema.json |
| S-08-86 | `aos daemon add` 只登記成 pending，沒有 run 被起 | 回傳 | 讀登記表與 `ps` |
| S-08-87 | `--register` 只改得動那四欄 | 回傳 | 前後比對 |
| S-08-88 | 登記表沒有 LLM 世界那筆時 start 自動補 | 回傳 | 讀登記表 |
| S-08-89 | 對帳完 pending 與 resume 都被起，`resume` 清掉 | 回傳 | 讀登記表 |
| S-08-90 | 子行程結束後沒有殭屍 | 回傳 | `ps` 看不到 defunct |
| S-08-91 | `--until never` 那筆退出後保留並重起 | 回傳 | 同 S-08-73 |
| S-08-92 | `aos daemon stop` 把 `resume` 設成 true | 回傳 | 讀登記表 |
| S-08-93 | 沒登記的 run 也殺得掉 | 回傳 | 讀 `.aos/lock` 的 pid |
| S-08-94 | 兩處都查不到活人 | 回傳 | 不投控制信，輸出列出查過哪兩處 |
| S-08-95 | `last_started_at` 是可選時間戳 | schema | registry.schema.json |
| S-08-96 | 沒有 `last_started_at` 的 every 筆立刻起 | 回傳 | 讀登記表與 `ps` |
| S-08-97 | `llm_world` 那個資料夾沒有 `.aos/` | 存在 | start 之後它有了 |
| S-08-98 | 四方各寫一次登記表都成功 | 回傳 | 四筆都在，都經過 `registry.lock` |

## 09 LLM 世界

| 條款 | 檢查怎麼做 | 種類 | 用什麼 |
|---|---|---|---|
| S-09-01 | 它有 `.aos/`，登記表裡有它一筆 | 存在 | 看檔與登記表 |
| S-09-02 | 預設在家的 `.aos/llm/`，改設定換得掉 | 回傳 | 同 S-02-27 |
| S-09-03 | daemon 起的是 `aos llm serve`，`runner` 是 llm-serve | 回傳 | `ps` 與登記表 |
| S-09-04 | 投一筆請求，結果落點出現檔 | 存在 | 看檔 |
| S-09-05 | 沒有把 LLM 當指令的路 | 人工 | 同 S-04-27 |
| S-09-06 | 同步子樹裡不准有請求 | 人工 | 同上 |
| S-09-07 | 跑完一輪沒有新的地被建出來 | 存在 | 比對目錄清單 |
| S-09-08 | 相對路徑以 `from` 那塊地為基準；指進 `.aos/` 就拒 | 回傳 | 退非 0 |
| S-09-09 | `format_version` 與 `kind` | schema | llm-request.schema.json |
| S-09-10 | 同 id 再投 | 回傳 | 同 S-07-39 |
| S-09-11 | `from` 與 `at` | schema | llm-request.schema.json |
| S-09-12 | `prompt` 是路徑不是內文 | schema | 同上 |
| S-09-13 | 請求裡沒有指定單元的欄 | schema | 同上 |
| S-09-14 | 假後端的輸出原封落在結果落點 | 回傳 | 比對內容 |
| S-09-15 | `priority` 必填整數 | schema | llm-request.schema.json |
| S-09-16 | 省略時用 `.aos/units.json` 的 `max_wait_ms` | 回傳 | 看退回的時間點 |
| S-09-17 | `tools` 是字串陣列 | schema | llm-request.schema.json |
| S-09-18 | 多一個怪欄位 | schema | 同上，不過 |
| S-09-19 | 一輪取件完收件匣空了 | 存在 | 看目錄 |
| S-09-20 | 三筆不同 priority 與 at，順序照排 | 回傳 | 比對落點的時間 |
| S-09-21 | 一輪派給一顆單元的筆數上限 | 回傳 | 數這一輪的 `aos llm` 次數 |
| S-09-22 | 一輪全部加起來的上限，兩個取小 | 回傳 | 同上 |
| S-09-23 | 它自己設得比 `.aos/units.json` 大 | 回傳 | 同 S-02-26 |
| S-09-24 | 沒有對得上 tier 的單元 | 存在 | 結果落點沒檔 |
| S-09-25 | 每筆派出去的跑一次 `aos llm`，成敗看落點 | 回傳 | 結束碼非 0 但落點有檔＝成功 |
| S-09-26 | 後端回 500 | 回傳 | 狀態檔 `backend_error`、有 `ext.retryable` |
| S-09-27 | 超過上限的還在隊裡，沒被退回 | 存在 | 沒有狀態檔 |
| S-09-28 | `max_wait_ms` 設小 | 回傳 | 狀態檔 `queue_timeout` |
| S-09-29 | 退回後換 id 再投跑得起來 | 回傳 | 看結果落點 |
| S-09-30 | 關掉一顆，還沒送出的改派同 tier | 回傳 | 讀帳簿的 `unit` |
| S-09-31 | 送出後斷線 | 回傳 | 假後端只被叫一次 |
| S-09-32 | `.aos/` 下沒有槽與工人那套檔 | 存在 | 同 S-02-14 |
| S-09-33 | `units` 正本在使用者層 | schema | config.schema.json |
| S-09-34 | 五個必填欄 | schema | 同上 |
| S-09-35 | `api_key_env` 只寫變數名 | 人工 | 機器分不出字串是名字還是金鑰 |
| S-09-36 | 通訊錄沒有端點欄 | schema | contacts.schema.json |
| S-09-37 | 一般世界寫 `units` | schema | `#/$defs/world`，不過 |
| S-09-38 | 三種收場各記一行 | 回傳 | 數帳簿行數 |
| S-09-39 | 刪掉帳簿照樣跑得動 | 回傳 | 刪掉再跑 |
| S-09-40 | 已寫的行不再變 | 回傳 | 前綴 `sha256sum` 比對 |
| S-09-41 | 每行十個欄 | schema | ledger.schema.json |
| S-09-42 | 行裡沒有 `format_version` | schema | 同上 |
| S-09-43 | `aos llm` 當過濾器直接跑 | 回傳 | 退 0 且有輸出 |
| S-09-44 | 五個旗標都在 | 回傳 | `aos llm --help` |
| S-09-45 | 四種退出碼各造一次 | 回傳 | 退出碼 |
| S-09-46 | 後端錯誤時 stderr 有狀態碼 | 回傳 | `grep` stderr |
| S-09-47 | 同時起兩支都跑得完，沒有限流 | 回傳 | 都退 0 |
| S-09-48 | 殺掉 daemon 再起，沒處理完的還在 | 存在 | 看收件匣 |
| S-09-49 | 重啟靠 `.aos/requests/` 認人 | 回傳 | 收件匣清空也認得出來 |
| S-09-50 | 收下之後才失敗的不回信 | 存在 | 請求者的收件匣是空的 |
| S-09-51 | 沒填 `timeout_ms` 時當 300000 | 回傳 | 同 S-02-47 |
| S-09-52 | 落檔那筆用的是單元的 `timeout_ms` | 回傳 | 讀落檔的指令 |
| S-09-53 | daemon 起它之前抄好 `.aos/units.json` | 存在 | 看檔 |
| S-09-54 | 跑的時候沒開過家的設定檔 | 回傳 | `strace -e openat` |
| S-09-55 | agent 算上限讀的是 `.usage.json` | 回傳 | 改帳簿不影響、改用量檔會影響 |
| S-09-56 | 四種退出碼都不決定要不要重試 | 回傳 | 同 S-09-57 |
| S-09-57 | 狀態檔有 `ext.retryable` | 回傳 | 讀狀態檔，schema 沒卡 |
| S-09-58 | 別的地登記 `aos llm` 當工具 | 回傳 | `aos tool add` 與 `aos check` 都退非 0 |
| S-09-59 | 併發只在 LLM 世界那邊數 | 回傳 | daemon 的 `max_parallel` 調大不影響 |
| S-09-60 | 成功時 `.usage.json` 六個欄 | schema | usage.schema.json |
| S-09-61 | `.usage.json` 先於結果檔出現 | 回傳 | 比對 mtime |
| S-09-62 | 失敗時沒有 `.usage.json` | 存在 | 看檔 |
| S-09-63 | 每筆請求有一份 `.aos/requests/<id>.json` | 存在 | 看檔 |
| S-09-64 | 那個檔的欄位與四種 `state` | schema | request-state.schema.json |
| S-09-65 | `aos status --request` 印得出來 | 回傳 | 退 0 且輸出有 `state` |
| S-09-66 | `state` 是 done 但落點沒檔 | 回傳 | 父還是判成沒好 |
| S-09-67 | 重啟時 sent 的那些 | 回傳 | 狀態檔 `result_unknown`、`retryable` 是 false |
| S-09-68 | 失敗後沒有第二次送出 | 回傳 | 假後端只被叫一次 |
| S-09-69 | done 的紀錄保留到期限才被刪 | 存在 | 看目錄 |
| S-09-70 | queued 與 sent 的紀錄沒被清 | 存在 | 同上 |
| S-09-71 | 請求者地上的三種檔都還在 | 存在 | 看檔 |
| S-09-72 | 兩個旗標都在，預設 200 與 never | 回傳 | `aos llm serve --help` |
| S-09-73 | serve 在跑時再起一支 | 回傳 | 退 75 |
| S-09-74 | 那塊地沒有 `main.aos.json` 也跑得動 | 回傳 | 退 0，沒有 `.aos/series.json` |
| S-09-75 | requests 裡兩條路徑是絕對的 | 回傳 | 讀檔；錯誤訊息提到基準 |
| S-09-76 | `tools` 只接在 prompt 前面 | 回傳 | 假後端收到的本體沒有那個欄位 |
| S-09-77 | 三個假後端各跑一次 | 回傳 | `echo:` 退 0、`fail:` 有狀態檔、`slow:` 慢 |
| S-09-78 | 整批檢查在沒有網路的環境跑得完 | 回傳 | 斷網跑一遍全過 |
| S-09-79 | `tokens_source` 兩選一 | schema | ledger.schema.json |
| S-09-80 | `outcome` 六選一，且跟狀態檔的 `reason` 對得上 | schema | 同上，加比對 |
| S-09-81 | 一輪裡壞了幾筆不影響 serve 的退出碼 | 回傳 | 退 0 |
| S-09-82 | `aos llm ask` 產的請求過同一份 schema | schema | llm-request.schema.json |
| S-09-83 | 請求寫壞的進隔離區並回信 | 存在 | 看 `rejected/` 與 `from` 的收件匣 |
| S-09-84 | 一輪派出去的筆數 | 回傳 | 同 S-09-21 |
| S-09-85 | 配不到單元的那筆 | 回傳 | `state` 是 queued，`ext.waiting_tier` 有值 |
| S-09-86 | 只有 smart 的請求、只有 fast 的單元 | 存在 | 一直排隊，落點沒檔 |
| S-09-87 | 排隊時間從投遞物的 `at` 算 | 回傳 | 改 `at` 就改退回時機 |
| S-09-88 | `queue_timeout` 的 `message` 有兩個數字 | 回傳 | 正規式對它 |
| S-09-89 | `request` 是原始投遞物的原文，處理完還在 | 回傳 | 比對兩份 json |
| S-09-90 | 同 id 再投靠 `.aos/requests/` 擋 | 存在 | `.aos/` 下沒有第二個去重檔 |
| S-09-91 | 一輪真的並行起最多那麼多支 | 回傳 | `ps` 數同時在跑的 `aos llm` |
| S-09-92 | 一輪開頭讀一次，等的時候 100 毫秒內再讀 | 回傳 | 量投停到停下來的時間差 |
| S-09-93 | 收到停時已送出的那幾支跑完 | 存在 | 它們的落點都有檔 |
| S-09-94 | serve 停下來寫停止原因檔 | schema | stopped.schema.json |
| S-09-95 | `--usage-out` 成功失敗都寫得出那五個欄 | 存在 | 看檔 |
| S-09-96 | `--request-id` 只進帳簿與用量檔 | 回傳 | 換 id 不影響回話內容 |
| S-09-97 | serve 派的每筆都帶那兩個旗標 | 回傳 | 讀落檔的指令 |
| S-09-98 | 兩份用量檔不是同一份 | 回傳 | 前者沒有 `format_version` |
| S-09-99 | `aos llm init` 建 `.aos/` 並寫 `units` | 存在 | 看兩個檔 |
