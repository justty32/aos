# 2026-09-13 晚整理回報

這輪只拆檔與對齊文件，不改行為；沒有 commit、push 或開 agent，也沒有碰 `playground/`。

## 拆檔

| 原檔 | 拆出的新檔 | 原檔行數 | 整理後行數 | 搬移內容 |
|---|---|---:|---:|---|
| `proto4-3/aos_run.py` | `proto4-3/aos_run_status.py` | 300 | 214 | `_State`、`_Status`、`_killpg()`、`_install()`：訊號狀態、status-fd 事件與 handler 安裝 |
| `proto4-3/aos_inst.py` | `proto4-3/aos_inst_resolve.py` | 296 | 105 | `InstError`、`abspath()`、`_envs()`、`_resolve()`、`_is_directive()`、`_apply()`、`_opt()`、`_str()`、`_lookup()`、`_fmt()`、`_ref()`、`_pointer()`：指示詞展開、參照與欄位驗證 |
| `proto4-5/llm_cpu_home.py` | `proto4-5/llm_cpu_request.py` | 291 | 232 | `read_json()`、`validate_id()`、`request_sha256()`、`existing_request()`、`existing_request_sha256()`：請求 ID、位置與穩定指紋 |
| `proto4-3/aos_daemon.py` | `proto4-3/aos_daemon_lifecycle.py` | 281 | 213 | `Daemon.save()`、`serve()`、`shutdown()`、`say()`：state 落地、前台主迴圈、收工與 log；改由 `_Lifecycle` 承接 |

新檔行數依序為 88、195、66、78，全部 ≤ 250。以 AST 擷取搬移前後的 class／function source 逐項比對，以上函式與方法內容全部逐字相同；只改 import 與 `Daemon` 的承接類別。用隔離的暫存 git index 納入新檔後跑完整 stat，結果為 17 files changed、333 insertions、532 deletions；四個原檔明顯縮小，內容對應進四個新檔，真實 git index 未動。

## 文件對齊

- `proto4-3/docs/files.md` 補上四個新拆檔，並補齊原本漏列的 `aos_kernel_module.py`；八個 `aos_kernel_*.py`（add／boot／init／module／schedule／status／syscall／tick）已逐一對帳，沒有多列或漏列。
- `proto4-3/docs/kernel.md` 的 config 七欄與現行 init／讀取一致；`state.json` 範例補上 `seen_runs`，並說明現行的 `waiting`／`wait_runs`、`bad_runs`／`bad_exit`、`aos_ticks` 暫存欄位。
- proto4-3 README 的「怎麼跑」內容未動，所有連結逐一確認存在。
- proto4-3／4-4／4-5／4-6 README 最後一節統一為 `## 測試與出處`，都標明 `test/` 並用「從 repo 根目錄 `cd proto4-X` 再執行」的指令格式。
- `wf/INDEX.md` 的 `proto*/` 單列改列四個現行原型、五站 `playground/` 與 `proto4/notes/`；code map 同步補上新檔職責。
- 根 `.gitignore` 已有 `__pycache__/` 與 `*.pyc`，三個 proto 底下的 cache 均受遮蔽，未修改 `.gitignore`。

## 五邊測試數字

| 邊 | baseline | 整理後 | 結果 |
|---|---:|---:|---|
| proto4-3 | 237 | 237 | 全綠 |
| proto4-4 `aos`／`step`／`cpu` | 42／48／12 | 42／48／12 | 全綠 |
| proto4-5 | 63 | 63 | 全綠 |
| proto4-6 | 77 | 77 | 全綠 |
| 根目錄 ctest | 8/8 | 8/8 | 全綠 |

根目錄兩次皆以 `cmake --build --preset default && ctest --preset default` 驗證；Python 與 Janet 指令也依任務指定執行。沒有新增或刪除測試。

## 看到但沒動的問題

- proto4-3 測試的 baseline 與整理後都出現同一則 Python 3.14 `ResourceWarning`：測試期間有一個 `subprocess` 物件被回收時仍顯示 running；237 tests 仍全綠。本輪不追修，避免把既有測試／行程行為混進整理。
- `wf/INDEX.md` 其他既有列仍把核心描述成只有 `core/inst`、`modules/` 為空，與目前 code map 的七個 core 不一致；本任務只授權改 `proto*/` 那列，因此沒順手整理。
- `proto4-3/aos_kernel.py` 的檔頭仍有一句「`aos-kernel` 這支只剩 `ls`」，但同檔現行仍分派 `add`／`rm`／module；指定對齊的 `docs/files.md` 與 `docs/kernel.md` 已正確，本輪沒有擴張去改該原始碼檔。
- 工作進行中另有未追蹤的 `proto4-7/` 與 `bob-q1-s1.req.json` 出現在共用工作樹；最終驗收後，`playground/README.md`、`playground/env.sh` 與 `playground/stations/6-agent/` 也被其他工作改動。它們都不屬本任務，沒有讀取、修改、回復或納入本輪 diff。
