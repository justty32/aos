# 試玩任務：你是第一次碰 aos 原型的新使用者

你在 repo `/home/lorkhan/repo/simple_tools/aos`。**你的角色是新來的使用者**，不是開發者。**只准讀這兩個入口與它們連出去的文件**：`proto4-3/README.md`（作業系統那層）與 `proto4-4/README.md`（第一個程式：逐步 lisp）。**不要去讀 `proto4/notes/`（設計筆記）、不要讀原始碼**——除非 README 讓你卡住、非讀不可，那也算一條「README 沒講清楚」的發現，要記下來。

**不要改 repo 裡任何檔案。** 你玩的東西全部放在 `/tmp/play-<你的名字>/` 底下。**不要 git commit／push**。不要開其他 agent。`janet` 在 `~/.local/bin/janet`（`export PATH="$HOME/.local/bin:$PATH"`）。跑 daemon 一律用暫存家（`AOS_DAEMON_HOME=/tmp/play-…/home`），玩完把你開的 daemon 停掉。

## 要玩什麼（照順序，每步記下「我以為會怎樣 vs 實際怎樣」）

1. **只看 README 把東西架起來**：daemon → kernel 的家 → 插 cpu → 看 `aos-kernel ls`。記：花了幾分鐘、卡在哪、哪個指令要回頭翻第二次。
2. **寫一個你自己的逐步 lisp 小程式**（不要抄 README 的例子）：至少五個 form，其中要有 `def`、`defn`、寫檔、用 `aos/call-dir` 叫一個你自己做的資料夾（資料夾裡放一份 inst.json，跑個 shell 指令產生 JSON），再用 `:read`＋`:json` 把結果讀回來用。先手動 `aos-step` 一格一格跑，再丟進 kernel 的 `procs/` 看它自己跑完、被收走。
3. **故意弄壞三樣**：inst.json 少寫 `cwd`／form 裡寫錯字／把正在跑的 prog.janet 前面插一個 form。看錯誤訊息你看不看得懂、知不知道下一步該做什麼。
4. **問自己五個問題**（這是使用者定的標準，每條 1–5 分＋至少兩個具體例子，例子要帶指令或檔名）：
   ① 容易上手：從零到第一個程式跑起來，要幾步、幾個指令？
   ② 容易理解：cpu／行程／kernel／inst.json／pc 這些詞，README 一講你就懂了嗎？哪個詞讓你想歪？
   ③ 複雜的東西有沒有藏好：有沒有哪個你本來不該知道的細節被迫知道（例如 `runs_at`、硬連結、`--home` 不會傳給子孫）？
   ④ 外層的控制結構簡單但全面：那幾支指令（init／add／ls／step／tick…）夠不夠用、有沒有重疊或缺口？
   ⑤ 要背的東西少：你得記住幾個檔名、幾個旗標、幾個環境變數才能日常使用？哪些可以合併或有預設？

## 交出來

一份 markdown 直接回給我（不要寫進 repo），繁體中文、大白話、150 行以內：

- 開頭：一句總評 ＋ 五條各幾分。
- 中間：照上面 1–4 的順序寫你的經歷與發現，每條發現用「現象 → 我期待 → 建議」三段一行。
- 最後：**前五名最該改的**（按「改了對新手幫助最大」排），每條一句話＋預估改動大小（小／中／大）。
- 附：你實際跑過的指令清單（精簡）。
