# 試玩任務 r2：你是第一次碰 aos 原型的新使用者（第二輪，驗證上一輪的修法有沒有真的解決）

你在 repo `/home/lorkhan/repo/simple_tools/aos`。**你的角色是新來的使用者**，不是開發者。**只准讀這兩個入口與它們連出去的文件**：`proto4-3/README.md`（作業系統那層）與 `proto4-4/README.md`（第一個程式：逐步 lisp）。**不要去讀 `proto4/notes/`（設計筆記、上一輪的報告都在那，不准看）、不要讀原始碼**——除非 README 讓你卡住、非讀不可，那也算一條「README 沒講清楚」的發現，要記下來。

**不要改 repo 裡任何檔案。** 你玩的東西全部放在 `/tmp/play2-<你的名字>/` 底下。**不要 git commit／push**。不要開其他 agent。`janet` 在 `~/.local/bin/janet`（`export PATH="$HOME/.local/bin:$PATH"`）。跑 daemon 一律用暫存家（`AOS_DAEMON_HOME=/tmp/play2-…/home`），玩完把你開的 daemon 停掉（`aos-daemon-ctl stop`）。

## 要玩什麼（照順序，每步記下「我以為會怎樣 vs 實際怎樣」）

1. **只看 README 把東西架起來**：從「上電」到看到 `aos-kernel ls` 的表。記：幾個指令、幾分鐘、哪個指令要回頭翻第二次、README 有沒有哪一句是多餘或缺的。
2. **寫一個你自己的逐步 lisp 小程式**（不要抄 README 的例子）：至少五個 form，其中要有 `def`、`defn`、寫檔、用 `aos/call-dir` 叫一個你自己做的資料夾（資料夾裡放一份 inst.json，跑個 shell 指令產生 JSON），再用 `:read`＋`:json` 把結果讀回來用。先手動 `aos-step` 一格一格跑，再**用 README 教的方式排進 kernel**看它自己跑完、被收走。排進去時 inst.json 的 `cwd` 與 `argv` 故意寫相對路徑，看會怎樣。
3. **故意弄壞五樣**，每樣記「畫面上看到什麼、我知不知道下一步」：
   - inst.json 少寫 `cwd`；
   - inst.json **不寫 `stderr`**，然後 form 裡寫個錯字，用 README 教的方法看到錯誤；
   - 把正在跑的 prog.janet 前面插一個 form；
   - daemon 沒開就去開機；kernel 的家沒灌就去開機；
   - 同一個名字排兩次。
4. **問自己五個問題**（使用者定的標準，每條 1–5 分＋至少兩個具體例子，例子要帶指令或檔名）：
   ① 容易上手：從零到第一個程式跑起來，要幾步、幾個指令？
   ② 容易理解：cpu／行程／kernel／inst.json／pc／boot 這些詞，README 一講你就懂了嗎？哪個詞讓你想歪？
   ③ 複雜的東西有沒有藏好：有沒有哪個你本來不該知道的細節被迫知道？
   ④ 外層的控制結構簡單但全面：那幾支指令夠不夠用、有沒有重疊或缺口？
   ⑤ 要背的東西少：你得記住幾個檔名、幾個旗標、幾個環境變數才能日常使用？

## 交出來

一份 markdown 直接回給我（不要寫進 repo），繁體中文、大白話、130 行以內：

- 開頭：一句總評 ＋ 五條各幾分。
- 中間：照上面 1–4 的順序寫經歷與發現，每條發現用「現象 → 我期待 → 建議」三段一行。
- 最後：**前五名最該改的**（按「改了對新手幫助最大」排），每條一句話＋預估改動大小（小／中／大）。如果你覺得已經沒有值得改的，直說。
- 附：你實際跑過的指令清單（精簡）。
