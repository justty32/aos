最值得撿的是 proto2 的跨格背景 worker、proto 的 `result_unknown` 不重送、llmkit 的請求／usage 正規化。  
報告已新增：[legacy-harvest.md](/home/lorkhan/repo/simple_tools/aos/proto4/notes/llm-cpu/legacy-harvest.md)，共 198 行。  
最不確定的是 `pi -p` 最終輸入／輸出契約，因此建議 v1 先保留 disabled 設定。  
報告本身的 key 掃描為空；整個目錄只命中既有 `harvest-task.md` 裡寫出的檢查指令，不是 key。  
沒有碰其他檔案，沒有連網、呼叫 LM Studio、commit 或 push。