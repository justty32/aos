# 整理任務（2026-09-13 晚）：不改行為的拆檔與對齊

目的：使用者今晚回家要玩，先把邊上的大檔拆開、文件對齊，**行為一個字都不能變**。

## 規矩

- 你在 repo `/home/lorkhan/repo/simple_tools/aos`。先讀 `AGENTS.md` 開頭三軸、`wf/STRUCTURE.md`（拆檔規則：程式碼與手冊單檔 > 300 行就拆）、`wf/workflows/dev-env.md`。
- 先跑 baseline 記數字：`proto4-3`（`cd proto4-3 && python -m unittest discover -s test`）、`proto4-5`、`proto4-6`（同法）、`proto4-4`（`cd proto4-4 && for t in aos step cpu; do janet test/$t.janet; done`；`janet` 在 `~/.local/bin`）、根目錄 `cmake --build --preset default && ctest --preset default`。做完全部重跑，**數字一模一樣、全綠**（這是整理，不加測試也不減）。
- **不要碰 `playground/`**、不要碰 `proto4/notes/`（除了本檔旁邊要你寫的回報）。
- 拆檔＝原文搬家：搬過去的函式內容不改一個字（可以改 import 與呼叫名），搬完 `git diff --stat` 要看得出是搬不是重寫。不順手「改善」任何東西，看到 bug 只寫進回報。
- **不 commit、不 push、不開 agent。** 最後 markdown 回報：每個檔拆成什麼、搬了哪些函式、五邊測試數字、看到但沒動的問題。

## 要做的

1. **邊上的大檔先拆**（300 行規則，`wc -l` 現況）：`proto4-3/aos_run.py` 300、`proto4-3/aos_inst.py` 296、`proto4-5/llm_cpu_home.py` 289、`proto4-3/aos_daemon.py` 281。每個找一塊完整職責搬到新檔（例如 aos_run 的訊號／狀態回報、aos_inst 的驗證器、llm_cpu_home 的請求指紋／範例檔、aos_daemon 的表讀寫），目標各回到 ≤ 250 行，留成長空間。新檔開頭一行說它負責什麼。**fix-r5 那個 codex 可能剛改過 `llm_cpu_home.py`、`llm_cpu_module.py`、`aos_kernel_status.py`、`aos_kernel_schedule.py`**——先 `git status`／`git log -1` 看工作樹，以當下內容為準。
2. **proto4-3 README 與 docs 對齊實況**：`docs/files.md`、`docs/kernel.md` 列的檔案／欄位跟現在的 `aos_kernel_*.py`（add／boot／init／module／schedule／status／syscall／tick）對得上嗎？少的補一行、多的刪。README 的「怎麼跑」不動內容，只確認連結沒斷。
3. **四份 README 的「出處／測試」節格式一致**：proto4-3／4-4／4-5／4-6 每份最後都有一節講「測試怎麼跑、檔在哪」，用同一個標題、同一個指令寫法。只動格式與指令正確性。
4. **`wf/INDEX.md` 的 `proto*/` 那列**：現在只說「現行是 proto4-3」。改成一句話帶四個現行原型（4-3 作業系統層、4-4 逐步 lisp、4-5 LLM 兩層、4-6 逐步 JSON／Python／Lua）加 `playground/`（給人玩的遊樂場，五站）與 `proto4/notes/`。一列講完，不展開。
5. **`__pycache__`**：三個 proto 底下都有，確認 `.gitignore` 有擋（有就不動）。

## 回報

寫到 `proto4/notes/tidy-out-2026-09-13.md`（也是唯一准你寫進 `proto4/notes/` 的檔），順便在回覆裡貼一份。
