# 簡化輪實玩

## 介面前後

- 以前：think、branch、bigmem、mail_wait、jobs 各自送請求、存等待狀態、輪詢結果。
- 現在：一律用 `ctx.send()`、`ctx.pending()`、`ctx.cancel()`、`ctx.sleep_until()`；只有共用層搬結果並叫 `on_result()`。
- 以前：各包自己解通訊錄、`.aos/inst`、父鐘與深度。
- 現在：一律用 `ctx.world_of()`、`ctx.home_of()`、`ctx.clock_of()`、`ctx.depth()`。
- 以前：new、spawn、team 各寫一套世界骨架。
- 現在：三個入口都走 `aos_agent.create_world()`。

## 刪除與行數

- 刪掉 `think.conclude`。工具總數 75 → 74；沒有新增工具。
- 程式 8,465 → 8,460 行，少 5 行。
- README 259 → 257 行；README 加 docs 1,813 → 1,607 行。

## deepseek-flash 實玩

- 深思題只叫一次 `think`，沒有輪詢工具。status 顯示 `在等 think <id>`，結果回來後自己醒並回答。
- 第一輪三分支因整題跑到 60 格，被硬上限停住；使用者回「繼續」後接著跑，不重開原分支。
- 第二輪 fork 三條時，睡著期間 status 顯示在等 branch；三條回來後只叫一次 `join`，直接交付。
- 實玩世界只登記自己的鐘；結束後已 unregister，共用 LLM 鐘沒有動。

## 還沒簡化到

- 每題格數目前把旁線睡眠也算進去；慢分支可能先撞 60 格，再等使用者說繼續。
- `branch` 若供應商回空內容，模型仍可能自行補一次 think；共用層只保證收齊，不替模型補內容。
- 旁線逾時用 agent 格數，不是牆上時間；世界完全沒鐘時則會立刻回錯誤。
