# 2026-09-07 小型工作室實玩（三）：預算五百萬，看交不交得出來

## 場地與結果

- 場地：`<scratchpad>/play-studio-3/studio`；引擎公司 ollama（qwen2.5:14b），共用上限 2。
- 隊預算 5,000,000 tokens／3 小時／20,000 ticks；訂單 3,000,000 tokens／2 小時；同一張 todo.py 單。
- 10:03 開跑，11:08 使用者手動中止（codex 原訂等到 75 分鐘）。**沒有交付**，`team/projects/` 與 `progress.md` 全空。
- 停下時：115,221 tokens（只用 2.3%）、10,749 ticks；owner 85,879、pm 22,131、sales 7,211（個人額度 0 仍能叫模型，同 B）；chief、dev-a、dev-b、qa 全程 0 token、從沒開工。
- token 在約第 6 分鐘後就靜止，之後 55 分鐘全是 idle 格空轉（每 5 分鐘約 +900 ticks）。LLM 端沒有 running／queued、沒有 error、沒有逾時；ollama 早已空閒，並發上限不是原因。

## 鏈斷在哪

- sales 收單、寄 PM、對甲方回「已提交 PM、等待分配」後就沒再更新，之後一小時對甲方的可見狀態與實況完全脫節。
- **PM 收到兩封信，`announced` 有記，但模型沒成功 `inbox_read`（只做過三次工具呼叫），信留在未讀箱；idle 掃描之後不再喚醒它。** 這是本輪唯一的斷點：不是預算、不是模型慢，是「已通知但未讀就永不重喚醒」。
- owner 跑了一段 56 格長迴圈、12 次工具呼叫，也留一封未讀；沒修 PM 的死信、也沒把任務降級直接派給 chief／dev。
- 沒有任何角色自行停鐘或向 sales 升級卡點，恢復策略等於零。

## 新坑

- `team status` 的 ticks 彙總在併發讀寫下曾從 6,895 倒退成 5,724、下一次又跳回單調；不能把它當單調計數器。
- 我的「連續 15 分鐘 tokens 與 ticks 都沒動就算卡死」抓不到這種狀態，因為 idle 格一直在加 ticks；該看的是「專案檔與未讀信 15 分鐘沒變」。

## 與 B 的對比與結論

- B（6 萬）和 C（500 萬）都停在同一個地方：PM 第一輪工具呼叫失敗後整條鏈斷。預算給夠也交不出來，瓶頸在信件生命週期與模型協定錯誤沒有重試，不在額度。
- 下一步（原本排給 codex 的任務 D，未跑）：① ticks 閘門每格都守、整隊 100% 全隊停並由 sales 自動問甲方追加；② 0 額度＝0 上限，`team new` 預設先分一小份給 sales；③ 模型把工具呼叫吐成壞文字時當協定錯誤、同題自動重送一次；④ `team_status` 顯示在途筆數；⑤（本輪新增）已通知但未讀的信要能重新喚醒 agent。
