# modules/ — 擴充小專案

可選的功能。跟 [`../core/`](../core/) 的小專案結構完全一樣，差別只在「拿掉它 aos
仍然成立」，而且可以用 `-DAOS_BUILD_MODULES=OFF` 整批不建。

目前還沒有擴充小專案。

新增小專案照 [`docs/subprojects.md`](../docs/subprojects.md)；`core/inst/` 是參考範本。
