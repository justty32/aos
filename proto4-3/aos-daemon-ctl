#!/usr/bin/env python3
"""aos-daemon-ctl 的命令列入口——薄薄一層，真東西在 aos_daemon_ctl.py。"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

import aos_daemon_ctl

if __name__ == "__main__":
    sys.exit(aos_daemon_ctl.main())
