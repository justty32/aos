#!/usr/bin/env python3
"""aos-llm-call 的命令列入口——實作在 ../lib/aos_llm_call.py。"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "lib"))

import aos_llm_call  # noqa: E402

if __name__ == "__main__":
    sys.exit(aos_llm_call.main())
