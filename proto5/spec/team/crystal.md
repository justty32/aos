← [team](README.md)

# 固化建議：`aos-team crystal`（第三波 W3-2）

門房（[route.md](route.md)）沒接住的話會落穿給領隊（模型）。如果同一種句型老是落穿、領隊每次都開同一種單，
這段其實可以寫成門房的規則，下次就不用叫模型。`crystal` 就是找這種句型、**提**候選規則給人批。
這是 ai_core §3.6「固化引擎」的最小版：**只提、不自動生效**（審查 S3）。

```
aos-team crystal [--min N] [--json] [--out FILE [--force]]
aos-team crystal --suggest-with-llm [--model ALIAS] [--min N] [--json] [--out FILE [--force]]
```

## 讀什麼

- `team/route.log`：門房每次的判決（讀不懂的行略過、數量印出來）。
- `team/post/sent/`（投遞紀錄）：人寄給領隊的那封 REQUEST 信。
- `team/tasks/`：領隊開的單。

**哪一句開出哪張單**：route.log 落穿給領隊的那行有 `letter`（那封信的 id；第三波 W3-2 起才有）就直接用；
舊 log 沒這格＝拿原文一樣、時間差 120 秒內、還沒被別行用掉的那封信（最近的）。
信 → 單用 `aos-team score` 同一個函式（[score.md〈起點〉](score.md)：這位領隊上一張單開單之後、這張開單之前的最後一封）。
所以一句話讓領隊開兩張以上的單，只算第一張；對不上信或單的，只列句子。
每句落穿標**可信度**（`high`／`medium`／`low`／`none`）；**有歧義（`low`）的只列、不拿來產候選**（astra 審查 S3，[crystal-checks.md](crystal-checks.md)）。

## 印什麼（機械版，預設）

1. **統計**：總句數、每條規則命中幾次、落穿幾次（分原因：沒命中／命中兩條以上／含否定詞／沒領隊）。
2. **沒命中的句型**：只拿「沒命中」的落穿句歸類（否定詞、命中兩條的，加規則也不會變；沒領隊的沒有單）。
   每類列次數、例句、每句領隊開的單（負責人、工作流、驗收條目種類、goal 開頭）。
   **領隊每次都開同一種單**的標出來。
3. **候選規則**與回測（下面），最後印提案檔路徑與「怎麼批」。

`--json` 印同一份資料。

### 句型怎麼歸類（決定性）

把句子裡會變的地方換成佔位、拿掉所有空白，剩下的字一樣＝同一類（骨架）。依序認：

| 佔位 | 認法 | 規則裡的群組 |
|---|---|---|
| 引號內文 | `「…」` 裡的字 | `q1`、`q2`…：`[^」]+` |
| 檔名 | 英數＋`_~./-`、以 `.副檔名` 結尾（前後不黏英數） | `f1`、`f2`…：**只收專案裡的相對路徑**，每一段以英數或底線開頭（擋 `/` 開頭、`~`、`-` 開頭、`..`）（第三波 W3-2 審查前補） |
| 路徑 | 英數＋`_~.-`、至少一個 `/`、沒副檔名 | `p1`…：同上 |
| 數字 | 前後不黏英數的一串數字 | `n1`… |

例：「把 a.md 改名成 alpha.md」「把b.md改名成beta.md」都是 `把{檔名}改名成{檔名}`。
只認英數的檔名與路徑（中文句子不用空白分字，沒法分出中文檔名）；沒加引號的自由文字不會變成佔位，所以「內容寫 今天下雨」每句都是不同類。
歸類認得寬、群組收得窄：`../x.md`、`/etc/a.md` 仍歸進同一類，但候選規則不會吃它，照樣落穿給領隊。

### 「同一種單」

負責人＋工作流＋驗收條目的種類（`check` 連名字，排序後比）一樣。**不比 goal 字面**：模型每次寫法不同。

## 候選規則（機械產生）

次數 ≥ `--min`（預設 2）、而且每句都對到單、單都是同一種的句型 → 產一條：

- `name`：`crystal-<類的序號>`（撞名加 `-2`…）；`do`：`handoff`。
- `pattern`：拿第一句例句，字面照抄（空白變 `\s*`、可有可無）、佔位換成上表的具名群組。
  這一類只有**同一句重複說**（不同寫法不到 `--min` 句）＝推不出哪裡會變，**整句照抄、不開群組**（印「整句照抄」）。
- `handoff`：挑一張領隊開的單當範本（goal 寫到每個佔位值的那幾句裡，挑「最短的值最長」的那句；都沒寫到＝goal 用人自己那句話），
  把 goal、done_when 裡的具體值換成 `{群組}`（檔名、數字前後不能黏英數，免得「1」吃掉 `worker-1` 的 1）。
  `assignee`、`workflow`、`max_attempts` 照抄；**`facts` 不抄**（那是領隊當時看到的專案狀態）。
- `tests.hit`：這類出現過的原句（去重）；`tests.miss`：別類落穿句裡不會被它吃到的前 3 句，再加一句「不要＋第一句 hit」（否定句）。

**機械檢查**（不過的不寫進提案，列在「丟掉」並說原因）：

1. handoff 裡的 `{名}` 都是 pattern 的群組；每句 hit 填好之後是合法的開單申請（跟 `ask` 真的開單同一套驗）。
2. 一條一條照順序加進「現有規則＋已收的候選」，**整份**（現有規則的例句也算）例句全過（`run_tests`，跟 `route test` 同一套）才收；兩條候選互相搶句子時先來的留下；寫檔前整份再跑一次。
3. **內建反例**吃到的丟掉（越界路徑、選項樣、追加指令…）。
（2、3 是 astra 審查 M3、M5 補的，細節 [crystal-checks.md](crystal-checks.md)。）

### 回測

每條候選拿 route.log 裡不是它例句的舊句子跑 `decide`，列它新吃到的句子，吃到別類或搶走別條規則的＝**疑似誤觸**（機械版標出來、照樣寫進提案；模型版丟掉）。細節 [crystal-checks.md](crystal-checks.md)。

## 提案檔

**完整的規則檔**：現有 `team/routes.json`（沒有就只有候選）＋收下的候選，寫到 `--out`（預設 `team/crystal/proposal-<時間>-<奈秒>-<pid>.json`，整檔原子寫）。
`--out` 不准是 `team/routes.json`（別名、符號連結、硬連結都算：退 1 `Refused`）；檔已經在＝不覆蓋、退 1 `AlreadyExists`，加 `--force` 才蓋（astra 審查 M2）。
候選的名字記在 `_metainfo.crystal.candidates`（另有 `source`：`mechanical`／`llm`、`made_at`、`note`）。
沒有候選＝不寫檔，印「沒有候選規則」。**`team/routes.json` 一個字都不動。**

怎麼批（印出來的就是這兩行）：

```sh
aos-team route test --file 提案檔
aos-team route save 提案檔
```

不要的候選，先從檔裡刪掉那條再批。

## `--suggest-with-llm`（加模型版，預設關）

改叫模型一次（`lib/aos_llm_ask.py`，設定讀 `AOS_LLM_CONFIG`，`--model` 挑代號，temperature 0）：
送現有規則的名字與 pattern、每句「沒命中」的落穿句與領隊開的單（負責人、工作流、goal、done_when），
請它歸納成同一種 routes.json 格式的規則。模型給的只當候選，**機械檢查兜底**，不過的丟掉並列原因：

1. 只收 `do: handoff`（`tool` 會直接跑指令，模型提的不收）；名字加 `llm-` 前綴。
2. 形狀對、pattern 編得過（`validate_routes`）。
3. **次數與單子從歷史算**，不數模型給的 hit：它的 pattern 在 route.log 吃得到的「沒命中」落穿句（可信度 `low` 不算）≥ `--min` 句，
   有群組的還要 ≥ `--min` 種寫法；那些句子每句都對到單、單都是同一種，而且負責人、工作流跟模型寫的一樣（astra 審查 M4）。
4. 同機械版第 1～3 條（群組對得上、填好合法、整份例句全過、內建反例）。
5. 回測**有疑似誤觸就丟**。

印出多一行：用了多少 token、幾毫秒、模型回了幾條。一樣只寫提案檔、人批。
模型回的不是 JSON＝沒有候選、印「讀不懂」，退 0。

## 退出碼

成功（不管有沒有候選）退 0；`--min` < 1、`--model` 沒配 `--suggest-with-llm`＝用法錯；團隊資料夾沒有 `team/`＝`NotFound`；
叫模型失敗照 `aos-llm call` 的代號（`EngineFailed`、`Timeout`…）退 1；`--out` 的問題見〈提案檔〉；
寫檔前整份例句沒全過（不該發生）＝`RoutesFailed`、不寫檔。
