← [team](README.md)｜公司：[company.md](company.md)｜帳戶：[cost.md §6](cost.md)｜報告：[2026-09-25-company](../../notes/2026-09-25-company/README.md)

# 市場層：幾家小公司競爭，經理人照表現撥額度

2026-09-25 第 1 版（組織設計總監）；同日兩家真跑一輪後修公式（研發部：失敗的單不算分、沒分數不准撥、快＝董事等的時間、省只比成功的、證據代號先展開）；同日晚第 74、75 題裁決（經理人，董事「都隨你」）：品質乘成功率與審查係數、快只在成功張數相同的之間比（§3）。程式：[`lib/aos_market.py`](../../lib/aos_market.py)；指令包裝 [`examples/company/market.py`](../../examples/company/market.py)。

董事長＝使用者（human），**經理人**＝調度者（Fable，不在 aos 裡）。幾家一樣的公司（[company.md](company.md)，`new --prefix c1-`…`c5-`）做同樣的單；
誰做得**好**（品管分）、**快**（秒數）、**省**（token）就多撥額度；花到 0 **倒閉**；剩兩家**合併**。
這支是經理人手上的**機械**指令：發多少照參數算，經理人可以覆寫；不叫模型。

## 1. 放哪

- **市場資料夾**（`--market`／`AOS_MARKET_HOME`，建議 `~/tmp/company-run/`）：`market.json`（參數、各家、每輪紀錄、事件）＋`archive/`（收掉的公司整個資料夾搬進來）。
- **帳戶**：財務部的 `$AOS_COST_HOME/accounts.json`（[cost.md §6](cost.md)）。一家公司一個帳戶，`root`＝公司資料夾；**配額**＝撥款加總，**已花**＝帳本裡落在公司資料夾底下的呼叫（kernel 兩池帶 `AOS_COST_HOME`，`company.py up` 會傳）。撥款、收回都是 `account_grant`（收回＝撥負的），所以 `accounts.json` 本身就是流水帳。
- 一家一個 kernel、一個 daemon（company.md §1、§6；董事 09-25）：五家同跑＝五個 kernel、五個 daemon，各在自己的公司資料夾裡（`K/`、`D/`），HR 只數得到自家。

## 2. 參數（`market.json` 的 `params`，人用文字編輯器改）

| 鍵 | 預設 | 意思 |
|---|---|---|
| `weights` | 品質 0.6、快 0.25、省 0.15 | 排名分＝三項（各 0～100）的加權和 |
| `round_pool` | 2 美元、5000 萬 token | 每輪撥出去的總額（約 10 張單） |
| `shares` | 35／25／20／12／8 ％ | 第 1、2…名拿幾成；家數少就取前幾個、按比例放大到 100％ |
| `seed` | 1 美元、2500 萬 token | 開辦費：約 5 張單（試玩 09-25：deepseek-chat 真跑一張「補人物」126 次呼叫、451 萬 token；舊預設 200 萬＝一張單就倒閉）。要多少請董事拍（報告問題 14） |
| `review_factors` | 1.0、0.7、0.4 | 品質的審查係數：製造部單子第 1／2／3 次審查才過（第 75 題；**起始值、可調**，超過 3 次用最後一個） |
| `min_quality` | 0 | 品質分（乘過成功率與審查係數之後）低於它＝這輪不撥（0＝不設） |
| `merge_at` | 2 | 營業中剩幾家才准合併 |
| `dept_order` | mfg、qa、rd、lib、hq | 合併時先收哪個部門的人 |
| `total` | 空 | **董事給的總量** `{"usd", "tokens"}`：設了才有「總池」，撥款不能超過（§4） |
| `machine` | 人頭 100、cpu 200、llm cpu 25 | 整台機器的名額：營業中各家 `limits` 加總不能超過（董事 09-25 14:20：llm cpu 20→25） |

## 3. 一輪

```text
經理人：market.py score c1 --eval <eval 結果.json>   （或 --quality 78；成功張數、董事等的秒數從公司的單與結案信算）
        market.py rank                              看排名（不寫帳）
        market.py bankrupt                          花光的先倒閉（倒閉名單要在撥款之前定）
        market.py grant [--usd c3=0]                照排名撥這一輪；--usd／--tokens 覆寫單一家（≥ 0）
        （剩兩家）market.py merge                    合併
```

**順序是先 `bankrupt` 再 `grant`**（astra 09-25）：花光的公司這輪一律不撥（排名照列、撥款 0），覆寫也不准給它（`Broke`）——否則撥款會把本來該倒的公司救活。

- **成功結案**（真跑 09-25 修；五家真跑後再修）：董事的一張單（`company.py order`）算**成功**＝總裁寄了結案信（前台部門寄給 human、DONE、第一行不是〔給 …〕），而且**品管過了**：
  - **優先看品管報告檔本身**：總裁結案信 `reply_to` 指到的那張品管總機單（沒有＝這段時間最後一張結案的品管總機單），照它任務單 `done_when` 裡 `qa-reports/` 底下的檔，第一個「結論：合格／不合格」（容「結論為：」「結論是:」）；合格、而且那張品管總機單 `done`＝過。總裁轉述寫錯（報告不合格、信說合格）照報告算。
  - 找不到報告檔（單子沒 `task`、檔不在、沒寫結論）才退回看結案信：信裡寫了「結論：合格」（容「結論為：」）、且這段時間有 done 的品管總機單。
  - FAILED、品管判不合格、沒經過品管的結案都算失敗。**被財務擋下的單**（郵差退件 FAILED「財務擋單：超支」，總裁照 SOP 回董事 FAILED）照這條自然算失敗。
  - **逾時＝失敗**：這一輪下的單（下單時間在上一輪 `grant` 之後）`score` 時還沒有結案信，算失敗一張（`timeout` 另記張數）；所以 `score` 要在這一輪的單都該結案之後才跑（[market-round.md](../../playbook/workflows/market-round.md) 的逾時）。算過逾時的單號記在 `market.json` 的 `timed_out`，之後才來的結案信不再算（同一輪重打分會重算）。
  - 配對：結案信的 `reply_to` 若是總裁發某張總機單的那封信，就配給那張總機單之前最後一張董事的單；其他照時間，每張還沒配的單配它之後第一封還沒配走的結案信（五家真跑 §7 第 6 條：先逾時的單會把後面的結案信搶走、秒數錯配）。
  - `score` 記 `done`（成功張數）、`failed`（失敗張數，含逾時）、`timeout`；經理人可 `--done N` 覆寫，手給 `--seconds` 沒給 `--done`＝算一張成功。
  - **成功率**＝成功張數 ÷ 董事下單張數（＝`done`＋`failed`，逾時在 `failed` 裡）；進品質分（下面「品質」，第 74 題）。
  - **審查輪數**（第 75 題）：每張成功的單，取那段時間最後一張有單號的製造總機單，看製造部那張單子的 `review`（郵差開的審查子單 `t-xxxx.r1`、`.r2`…的結果，一次一筆 pass）第幾次才過；單子 `failed`＝`FAILED`。`score` 記 `review_rounds`（照成功的順序）與 `review_factor`（照 `review_factors` 換算再平均）。找不到單子或沒有審查紀錄（舊資料、手給的成功）＝當一次過（1.0），`score` 的說明與 `rank` 那列都寫「沒有審查紀錄」。
- **這輪沒有成功結案（`done`＝0）的公司：品質、快、省、排名分全 0，撥 0**——不只靠 `min_quality`（真跑：c2 單子 FAILED，品質照樣 100、分到四成）。`rank` 那一列註明「這輪沒有成功結案：全項 0」。
- **分數綁輪次**：`score` 記下的分數帶 `round`（＝下一次 `grant` 的輪次）；`grant` 之後上一輪的品質、秒數、結單數都不算，這輪沒 `score` 就是 0。
- **品質**（0～100）＝**原始品質 × 成功率 × 審查係數**（第 74 題選「品質乘成功率」、第 75 題選「審查輪數算進品質」；經理人 09-25 晚裁決，董事「都隨你」）。審查係數：一次過 ×1.0、第二次過 ×0.7、第三次過 ×0.4、沒過（FAILED）×0——係數是**起始值、可調**（`review_factors`）。`rank` 印原品質、成功率、審查係數、調整後品質四欄。真跑：只成一張的公司不會再只差幾分（五家重算見 [rescore-74-75](../../notes/2026-09-25-company/market-run-5/rescore-74-75.md)）。
  **原始品質**：`score --eval` 讀 arknights 評分器的結果檔，每人 機械 40％＋證據列 40％＋評審 20％（沒評審就前兩項各 50％）取平均；或經理人直接 `--quality`。手給的數要有限：品質 0～100、秒數與跳數 ≥ 0。
  證據列有「無檔名」的：先把證據檔（結果檔裡的 `cand_root`，沒有就用公司的專案）裡「行號依據 A 檔 `…`」再寫「A L30」這種**代號展開成檔名**（暫存副本，不動原檔），用同一支 `evidence_check` 重跑；展開不了（找不到證據檔、沒有代號定義）＝證據那一項照原樣算（通常 0），`score` 的輸出寫「說明：…展開不了（原因）」。真跑：c1 從 50 回到 93.75（證據 7/8）。
- **快**＝**董事等了多久**（真跑 09-25 修；以前是部門間總機單的平均，c1 董事等 401 秒卻記 182 秒）：這一輪（上一輪 `grant` 之後，看結案信的時間，帶時區比）**成功**的董事單，從董事下單（前台部門 human 寄件格裡那封 REQUEST）到總裁結案信的秒數取平均；**只在成功張數最多的幾家之間比**（第 74 題，經理人 09-25 晚）：它們裡面最快的 100，其他＝最快 ÷ 自己 ×100；成功張數比較少的「快」＝0，`rank` 那列註明「不比快」（五家真跑第 2 輪：只成一張的 c2，唯一那張剛好最快，就拿了快＝100）。跨輪完成的單算在結案那一輪。跳數（那段時間裡的總機單數＋1）只記、不算分。
- **省**：這一輪花的 token（這輪已花 − 上輪 `grant` 時記下的已花），**只在成功的幾家之間比**：最省的一家 100，其他＝最省 ÷ 自己 ×100；成功、這輪沒花 token＝100。失敗的不進比較（它花得少不會拉低別人）。
- **只剩一家成功**：它的快、省都是 100，`rank` 那一列註明「省、快：無對照（這輪只有它成功）」——這一輪其實沒比出什麼。
  注意（品管部 09-25）：並行的單 `aos-team score --task` 報的是整隊的數；市場用的是**整家公司這一輪**的 token，不照單切，所以不受影響；要照單比就得照成員切。
- **這輪沒分數不准撥**（真跑 09-25 修）：這一輪一家都沒 `score`＝`grant`（連 `--dry-run`）退出非 0、印「本輪無分數」、什麼都不發。以前撥完一輪什麼都沒做再 `grant`，第 2 輪照樣整份發出去。有人 `score` 但全都失敗＝照公式全部撥 0（這一輪照樣結束、記進 `history`）。
- **同分**：同名次（名次跳號，例 1、1、3），那幾個名次的 `shares` 加起來平均分，不照公司名字破平。
- **撥**：`shares` 照名次分 `round_pool`；這輪沒成功結案的、`min_quality` 沒過的、花光的拿 0，其他人按比例放大；`--usd 名=X`／`--tokens 名=N` 覆寫（≥ 0；收回走 `close`）。美元一律**往下**取到 0.0001（不四捨五入，縮額後合計不會超過總池）。撥款記進帳戶（note「第 N 輪 第 k 名」，帶操作 ID），這一輪的排名、撥款、各家已花記進 `history`。

## 4. 總池（董事 09-25 追加）

- **錢**＝`total` − 各家已花（連收掉的）− 營業中各家手上沒花的配額（餘額 > 0 的部分）。等於「董事給的 − 已經撥出去還收不回來的」。
- **名額**＝`machine` − 營業中各家的 `limits`（人頭、cpu、llm cpu）加總。
- `open` 的開辦費、`grant` 的每輪撥款都從總池出：總池不夠，`open` 拒絕（`PoolEmpty`／`NoSlots`），`grant` 照比例縮到剛好發完（`history` 記 `capped`）。
- 公司收掉時，**它手上沒花完的配額**（任一種 > 0 的）撥負的收回、**它的名額**（`limits`）不再算營業中——兩樣都回到總池，經理人拿去撥給別家：
  - **倒閉**（`bankrupt`，某一種餘額 ≤ 0）：歸零的那一種沒得收；另一種還有剩就收回。**只是歸零倒閉的，回收的通常只有名額。**
  - **裁撤**（`close 名`，經理人決定收掉一家還沒花光的）：兩種剩多少收多少。
  - `bankrupt`／`close` 都回傳並記進 `events`：`recycled`（收回多少錢）、`slots`（放出幾個名額）、`freed`（放出的成員名，前綴可以再用）。
  - 印出來的「收回總池」只列 `total` 有設的那幾種（沒設 `total.usd`＝總池不管美元，就不印美元；帳照樣記在 `recycled`）；`ls` 的餘額不印 `-0.0`（五家真跑 §7 第 7 條）。
- 名額撥給別家：`slots 名 --llm-cpu 1`（`limits.llm_cpu`、`pools.llm` +1）、`--cpu 1`（`limits.cpu`、`pools.default` +1）、`--regular 1`；**只收 ≥ 0**（名額只在公司收掉、確定停好時回總池，不能先降帳面上限把名額虛增回來）；不能超過那家的 `limits_max`。只改 `company.json`，kernel 開著要 `aos-kernel cpu add`，或下次 `up`（`up` 會把 K 的池對到 `company.json`）。
- `pool` 印總池兩行。

**五家同跑的 llm cpu**：機器頂已提高到 25（董事 09-25 14:20 拍板，原 20 不夠五家滿編），每家新創預設 5 → 五家剛好 25，`open` 不用再降到 4。

## 5. 倒閉、合併的細節

**倒閉／裁撤**：先標 `closing`（記下封存目的地、操作 ID、名額，存檔；這時**還占著名額與錢**）→ 停（`company.py down`：撤總機、各部門 `aos-team stop`、`aos down`；**沒停好＝`StopFailed`，停在 `closing`**，不收回、不封存、不放名額）→ 停好後才讀餘額、收回沒花完的（還在跑的單記的帳都進來了）→ 公司資料夾整個搬進 `archive/<名>-bankrupt-<時間>/` → 市場標 `bankrupt`／`closed`。帳本、帳戶不刪（財務要查）。收掉的名字不能再開（舊帳戶帶著它的撥款與花費）；新公司的資料夾也不能跟任何帳戶的資料夾重疊（同一個、包著、被包著，連收掉的公司的原路徑都算），不然帳會重複算。
財務部那邊：帳戶倒閉後郵差本來就不再派新單（cost.md §4），就算經理人還沒跑 `bankrupt`，那家也不會繼續花大錢；已在跑的單做完為止。被擋的開單退件（FAILED「財務擋單：超支」），總機轉回總裁、總裁回董事，那張董事的單就結案（算失敗）——09-25 五家真跑前是留在 outbox，總裁不知道、董事的單永遠不結案。

**合併**（營業中剩 `merge_at` 家；`--force` 硬來）：排名高的併掉排名低的（`--into` 可指定），`--dry-run` 先看計畫：

1. 同部門的**經理（`template: lead`）只留併入方的**：被併方的經理裁掉（家跟著封存；筆記 `notes.json` 抄一份到併入方 `team/notes/_merged/<舊名>/` 備查）。
2. 其餘成員照 `dept_order` 併進併入方**同一個部門的團隊**，改名 `<併入方前綴><原職位>-<被併方名>`（`c2-mfg-writer1` → `c1-mfg-writer1-c2`；撞到併入方既有的名字或這次排好的就加 `-2`、`-3`，不蓋掉原成員）；`mail_to` 是那部門的經理＋human，經理的 `mail_to` 也加上他。
3. 併入方的正式名額（`limits.regular` − 現有正式）還有＝**正式**；滿了＝**臨時工**（名冊那一列寫 `employment: temp`，不算人頭）。被併方本來就是臨時工的仍是臨時工。
4. 併入方沒有那個部門的團隊＝那些人裁掉。
5. **跨任務記憶帶過去**：`team/notes/<舊名>/notes.json` 抄到新名底下；對話紀錄（家的 `prompts/`）留在被併方的封存，不搬（家裡有絕對路徑）。
6. 帳：被併方**停好之後**才讀它的餘額，一筆轉帳給併入方（`account_transfer`：同一次讀寫兩邊一起記，不會只做一半），被併方標 `merged`、封存；它的名額回總池。
7. 併入方 kernel 開著：動到的部門 `aos-team init`＋`start`，新成員開工。

實際順序：在市場鎖裡**重算**計畫（跟 `--dry-run` 看到的不一樣＝`Stale`，重看再合）→ 記進 `market.json` 的 `pending`、被併方標 `merging` → 停被併方 → 搬人（拿名冊鎖）→ 開工 → 轉帳 → 封存 → 結案。

## 5a. 鎖與崩了怎麼辦（astra 09-25）

- 改東西的子命令（`open`、`score`、`grant`、`slots`、`bankrupt`、`close`、`merge`）都拿 `<市場>/.market.lock`：兩個經理人指令不會同時讀到舊的總池各撥各的。
- `grant`／`close`／`bankrupt`／`merge` 都是「先把要做的記進 `market.json`（`pending`，或公司的 `closing`），再動手」；帳戶的每一筆撥款、收回、轉帳都帶**操作 ID**，同一個 ID 只記一次。崩在中間＝**重跑同一個指令接著做**（`merge` 不帶名字就接上次那筆），不重撥、不憑空多出額度。
- `pending` 沒清掉之前，別的 `grant`／`merge` 會拒絕（`Pending`），先把那筆做完。

## 6. 沒做的

- 真的五家同跑（09-25 只真跑過兩家一輪，見 [market-run](../../notes/2026-09-25-company/market-run/README.md)；其餘用假帳本測）。
- 品質分只接 arknights 評分器；別的產品線要自己的 `quality_from_eval`。
- `slots` 不會自己 `aos-kernel cpu add`（下次 `up` 才對齊）；倒閉不會自己把名額撥給誰（經理人撥）。
- 合併只合兩家；三家以上一次併要跑兩次。
