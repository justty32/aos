# 請求身分與送件恢復

> 等 agent 規範重寫後再判。

**問題**：agent 的 think／act 送出請求是三步：寫請求檔進 cpu、往 waits 加結果檔、寫 state（舊架構的說法；新架構下 agent 改成向 kernel `add --once`，見 [kernel §8](../spec/kernel.md)，三步怎麼變要看 agent 重寫）。中間崩掉會：再送一次（多問一次模型、工具重跑）、或永遠等（waits 加了但請求沒送）。目前只靠「結果檔在不在」分辨送出／收回，state.json 不記請求編號。

**為什麼現在不做**：原型跑得通，代價只是偶爾多做一次；加請求身分就要加對帳程式（llm cpu 6，2026-09-22 拍板先不加）。cpu／kernel 這層重架構後已有名字不重用、開機對帳、ack（[cpu §1、§6.2](../spec/cpu.md)），缺的只剩 agent 這層。

**以後從哪下手**：proto5.1 findings #11～13、#23、#26 已評估過三條路（請求編號、送件恢復、消費紀錄）。真的需要「崩潰後精準續跑」再開。
