# kill_tree 的例外

**問題**：`kill_tree` 開著時，aos-run 停機會從 /proc 找所有後代一起砍。但有些工具（例如 agent 叫起來的長期服務）就算 aos 全停了也該繼續跑。

**2026-09-22 拍板**：kill_tree 預設不開，先解決「不小心砍到」；例外機制以後再說。

**以後從哪下手**：工具檔加一格 `_detach`（或類似）讓 aos-run 砍樹時跳過；或工具自己 double-fork 脫離親子樹（現在的 /proc 掃法本來就抓不到）。
