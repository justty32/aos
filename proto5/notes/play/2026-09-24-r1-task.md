你是一個**第一次看到 aos proto5 的使用者**。repo `/home/lorkhan/repo/simple_tools/aos`。用繁體中文。可以開 subagent。

規則：**只准從 `proto5/README.md` 進去**，順著它的連結讀（`proto5/spec/*.md`、`proto5/lib/README.md`、cli）。**不准讀** `proto5/notes/`、`proto5/backlog/`、`proto5.1/`、`thinking/`、`wf/`、任何 `*-report*`／`*-changes*`／`*findings*`／`*play*`。程式碼可以看，但先照文件試，撞牆了才翻。

工作目錄：/tmp/claude-1000/-home-lorkhan-repo-simple-tools-aos/f594f460-c0a3-4df1-b41f-e70375230e84/scratchpad/play-astra/work/（家都建在這裡，**不要在 repo 裡建家、不要改 repo 任何檔**）。

要玩的：
1. 照文件把 daemon → kernel 架起來，丟一個 once 工作、一個反覆行程，看回音、`ls`，停機。
2. 照文件手建一個 agent 家（本機 LM Studio `http://localhost:1234/v1`、模型 `google/gemma-4-e4b` 已載著），給它一個工具（例如 `date`），`aos-agent start`、投一份輸入、看它走完 idle→think→act→think→idle、`aos-agent stop`。
3. 故意弄壞幾樣：info.json 少欄位、工具檔壞掉、llm.json 指錯 endpoint、模型逾時很短、tick 中途 `kill -9` 再 tick——錯誤訊息讀不讀得懂、能不能自己修回來。
4. 寫一支很小的「第二個工具」接上去。

最後一則訊息就是報告（≤ 4000 字，我會存進 repo）：五條標準各打分 1～5＋舉例（**容易上手、容易理解、複雜的東西藏好了、外層控制結構簡單但全面、要背的東西少**）；「卡住的地方」逐條（哪份文件、哪一步、你以為是什麼、實際是什麼）；「改了會更順的清單」按痛度排序最多 12 條；最後一句「你會不會想用它」。跑完 `pgrep -fa 'aos-cpu|aos-daemon|aos-kernel|aos-agent'`（排除 zsh -c／pgrep／codex）要空。不開 GUI、不截圖。卡超過 15 分鐘就記下來換下一步。
