你是一個**第一次看到 aos proto5 的使用者**。repo `/home/lorkhan/repo/simple_tools/aos`。用繁體中文。可以開 subagent。

規則：**只准從 `proto5/README.md` 進去**，順著它的連結讀（`proto5/spec/*.md`、`proto5/lib/README.md`、cli）。**不准讀** `proto5/notes/`、`proto5/backlog/`、`proto5.1/`、`thinking/`、`wf/`、任何 `*-report*`／`*-changes*`／`*findings*`／`*play*`／`fix-*`。程式碼可以看，但先照文件試，撞牆了才翻。

工作目錄：/tmp/claude-1000/-home-lorkhan-repo-simple-tools-aos/f594f460-c0a3-4df1-b41f-e70375230e84/scratchpad/play3-astra/work/（家都建在這裡，**不要在 repo 裡建家、不要改 repo 任何檔**）。LM Studio 在 `localhost:1234`、模型 `google/gemma-4-e4b` 已載著。

要玩的：
1. 照 README 從頭走到尾（含新的 `aos-agent init`／`say --wait`／`status`／`continue`），記每一步是不是照抄就過。
2. 模擬「早上開機」：daemon＋kernel 開好、三個 agent 用 `init` 生出來、各給不同人格與工具、`start` 三個；像聊天一樣輪流 `say` 給三個各三句（有的 `--wait`、有的不等，之後用 `status`／`last` 看）；中途把一個弄到連敗暫停（llm.json 改壞再改回），`status` 看得出來嗎、`continue` 救得回來嗎。
3. 故意弄壞：`say` 給沒 `start` 的 agent、`init` 在已有家的地方、`status` 在不是 agent 的資料夾、把 llm cpu `kill -9`、把 kernel 家的 `requests/` 整個刪掉——訊息讀不讀得懂、能不能只靠文件修回來。
4. 寫一支小工具接上去（例如「列出資料夾內容」），讓模型用它做一件事。

最後一則訊息就是報告（≤ 4000 字，我會存進 repo）：五條標準各打分 1～5＋舉例（**容易上手、容易理解、複雜的東西藏好了、外層控制結構簡單但全面、要背的東西少**）；「卡住的地方」逐條（哪份文件、哪一步、你以為是什麼、實際是什麼）；「改了會更順的清單」按痛度排序最多 12 條；最後一句「你會不會想用它」。跑完 `pgrep -fa 'aos-cpu|aos-daemon|aos-kernel|aos-agent'`（排除 zsh -c／pgrep／codex／`play3-opus`）要空。不開 GUI、不截圖。卡超過 15 分鐘就記下來換下一步。
