你是一個**第一次看到 aos proto5 的使用者**。repo `/home/lorkhan/repo/simple_tools/aos`。用繁體中文。可以開 subagent。

規則：**只准從 `proto5/README.md` 進去**，順著它的連結讀（`proto5/spec/*.md`、`proto5/lib/README.md`、cli）。**不准讀** `proto5/notes/`、`proto5/backlog/`、`proto5.1/`、`thinking/`、`wf/`、任何 `*-report*`／`*-changes*`／`*findings*`／`*play*`／`fix-*`。程式碼可以看，但先照文件試，撞牆了才翻。**不改 repo 任何檔。**

PATH 用**這個 worktree**的 `proto5/cli`（絕對路徑，不要用別的 worktree 或別的 checkout）：
`export PATH=/home/lorkhan/repo/simple_tools/aos/.claude/worktrees/agent-acb31b8cef4a19166/proto5/cli:$PATH`

工作目錄（家都建在這裡，**不要在 repo 裡建家、不要改 repo 任何檔**）：
- 若你是 Opus 玩家：`/tmp/claude-1000/-home-lorkhan-repo-simple-tools-aos/f594f460-c0a3-4df1-b41f-e70375230e84/scratchpad/play5-opus/work/`
- 若你是 astra 玩家：`/tmp/claude-1000/-home-lorkhan-repo-simple-tools-aos/f594f460-c0a3-4df1-b41f-e70375230e84/scratchpad/play5-astra/work/`

模型端點是 LiteLLM：`http://localhost:4000/v1`、模型 `deepseek-chat`、不用 api_key（雲端）。**絕對不准碰 LM Studio、`lms`、`localhost:1234`、ollama**——使用者在打遊戲，那顆 GPU 是他的，連測都不要測。

要玩的：

1. 照 README 從頭走到尾（含「每天重開機」那節），記每一步是不是照抄就過。三支指令 `aos-daemon`／`aos-kernel`／`aos-agent` 都用 `--target DIR`；daemon／kernel 用環境變數（`AOS_DAEMON_HOME`／`AOS_KERNEL_HOME`）省略 `--target`，agent 家每次都寫 `--target`。這輪重點驗（fix-r5 新東西，其餘沿用 fix-r4）：
   - `--target` 慣例：省略、環境變數、明寫三種都試一次，錯誤訊息尾巴有沒有講清楚這次用的是哪個家、從哪來的。
   - `aos-daemon boot`／`halt`。
   - `aos-kernel init --target K --config <檔>`、`aos-kernel halt`（等排程停、cpu 退出才回，`--no-wait` 也試）。
   - `aos-kernel check --probe`（真的打一次 endpoint；不帶 `--probe` 只驗設定，最後一行該寫「未測模型連線」）。
   - `aos-agent say TEXT --target DIR [--wait 秒]`。
   - `aos-agent listen --target DIR [--last|--wait [秒]|--follow]`。
   - `aos-agent pause --target DIR`／`continue --target DIR`／`continue --all`。
   - health 三個新階段：恢復中（llm cpu dead，daemon 重拉中）、重試中（連敗 N/3）、已解除暫停等下一次成功——這三種要自己好，觀察它們會不會真的自己變回 ok。
   - 重複 `start` 在已登記時退 0（印 `already started`）。
   - `aos-kernel init --force`（非空又不是 agent 家的資料夾）。
   - `aos-llm call`。

2. 模擬「早上開機」：daemon＋kernel 開好、三個 agent 用 `init` 生出來、各給不同人格與工具、`start` 三個；像聊天一樣輪流 `say` 給三個各三句（有的 `--wait`、有的不等，之後用 `status`／`listen` 看）；中途把一個弄到連敗暫停（llm.json 的 port 改壞再改回），觀察 `status`／`ls` 第一行從「重試中（連敗 N/3）」到「連敗暫停」到 `continue`（或 `continue --all`）後「已解除暫停，等下一次成功」再到「已恢復」的完整變化。

3. 故意弄壞（每種都做，卡超過 15 分鐘就記下來換下一步）：
   - `say` 給沒 `start` 的 agent（不帶 `--wait` 和帶 `--wait` 都試，看訊息、退出碼、話有沒有真的投進去）。
   - `init` 在已有家的地方（agent 家，應該拒絕）。
   - `aos-kernel init` 在非空又不是 agent 家的資料夾（應該退 1 要 `--force`）。
   - `status` 在不是 agent 的資料夾（例如指到 kernel 家）。
   - 把 llm.json 的 port 改壞，兩個 agent 同時在說話，連敗到暫停，觀察三個階段的訊息。
   - 把 llm cpu `kill -9`（正在工作跟閒著各試一次），觀察「恢復中」這句話。
   - 把 kernel 家的 `requests/` 整個搬走，`ls`／`status`／`say --wait` 的反應。
   - **新增**：agent 在 `pause` 中 `say`，話有沒有照收、提示對不對；`continue` 之後那句話有沒有被處理。
   - `check --probe` 在 port 錯的情況下要立刻 bad；port 對但模型清單沒列出這個模型時應該只是 warn（如果你能造出這個情境就試，造不出來就跳過並記下來）。

4. 寫一支小工具接上去（例如「列出資料夾內容」），讓模型用它做一件事。

最後一則訊息就是報告（≤ 4000 字，我會存進 repo）：五條標準各打分 1～5＋舉例（**容易上手、容易理解、複雜的東西藏好了、外層控制結構簡單但全面、要背的東西少**）；「卡住的地方」逐條（哪份文件、哪一步、你以為是什麼、實際是什麼）；「改了會更順的清單」按痛度排序最多 12 條；最後一句「你會不會想用它」。

跑完只看你自己這組的行程：Opus 玩家跑 `pgrep -fa 'play5-opus'`、astra 玩家跑 `pgrep -fa 'play5-astra'`，要空（同時還有別隊在跑東西，例如 `play5-opus` 或 `play5-astra` 以外的行程都不是你的，別碰、別殺）。不開 GUI、不截圖。
