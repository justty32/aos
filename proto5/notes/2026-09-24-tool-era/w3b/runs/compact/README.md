← [notes 總覽](../../../../README.md)｜規範 [compact-summarize.md](../../../../../spec/agent/compact-summarize.md)

# T-compact `--summarize` 真跑：機械版 vs 模型濃縮（第三波 W3-2）

## 怎麼跑

`python3 proto5/notes/2026-09-24-tool-era/w3b/runs/compact/live.py 5 ~/tmp/w3b-compact/main`（repo 根目錄；LiteLLM `http://localhost:4000/v1` 的 `deepseek-chat`，沒碰 LM Studio／ollama）。
一套 daemon＋kernel；每一次都是**全新的 agent 家**（`init`＋`tools add base`、`info.compact: false` 關掉自動壓縮），機械版與 `--summarize` 交替跑：

1. 用 read 讀 `workspace/long.txt`（40 行）、說幾行 ② 記住「最喜歡的水果是芒果」 ③④⑤ 三段約 300 字的自我介紹
2. `compact --keep-rounds 1 --max-tokens X`（X＝dry-run 往下找「①②兩輪都封存」的最大值，實際 846～1036）；模型版加 `--summarize`
3. 問「剛才 long.txt 幾行？不要再讀檔、不要用工具…不記得就說不記得」→ 問「我最喜歡什麼水果？」

原始結果 [results.jsonl](results.jsonl)（一次一行，含兩題原話）；每次的封存摘要全文在 [digests/](digests/)（`sum-N.mech.txt`＝同一份原文的機械版，拿來對照模型有沒有編東西）。

## 結果（10 次：機械 5、`--summarize` 5）

| 版本 | 次 | 行數題 | 水果題 | 壓縮後記憶 token（粗估） | 壓縮後第一問 prompt_tokens | 封存段 bytes | 濃縮那次模型 token（prompt＋completion） | 模型 ms | 壓縮指令秒數 |
|---|---|---|---|---|---|---|---|---|---|
| 機械 | 1 | 對 | 對 | 794 | 2227 | 1526 | — | — | 0.04 |
| 機械 | 2 | **答「不記得」** | 對 | 935 | 2330 | 1954 | — | — | 0.04 |
| 機械 | 3 | 對 | 對 | 895 | 2297 | 1858 | — | — | 0.04 |
| 機械 | 4 | 對 | 對 | 842 | 2262 | 1799 | — | — | 0.03 |
| 機械 | 5 | 對 | 對 | 786 | 2214 | 1660 | — | — | 0.03 |
| summarize | 1 | 對 | 對 | 517 | 2001 | 727 | 660＋131 | 1214 | 1.27 |
| summarize | 2 | 對 | 對 | 519 | 1999 | 727 | 630＋129 | 1334 | 1.39 |
| summarize | 3 | 對 | 對 | 482 | 1975 | 618 | 633＋101 | 929 | 0.99 |
| summarize | 4 | 對 | 對 | 473 | 1962 | 609 | 643＋100 | 1048 | 1.10 |
| summarize | 5 | 對 | 對 | 487 | 1980 | 643 | 652＋114 | 1196 | 1.25 |

平均：機械 記憶 850、第一問 2266、0.036 s；summarize 記憶 496、第一問 1983、1.20 s，模型 644＋115 token、1.14 s。
**5 段送模型、5 段都過機械檢查（沒有退回），模型沒出錯。** 5 份模型摘要逐句對過機械版，沒有編出機械摘要裡沒有的事（「六十公里」寫成「60 公里」是照提示改寫的數字）。

- 答對：機械 行數 4/5、水果 5/5；summarize 行數 5/5、水果 5/5。機械那次錯是保守答「不記得」（摘要裡明明有 `read（40 行）`、`回話：檔案 long.txt 共 40 行`），不是亂編——跟第一波 §9 看到的一樣（那次 5/5 都答不記得）。模型版把它寫成白話「結果是 40 行」，模型就敢照著答。**樣本只有 5 對 5，4/5 vs 5/5 不能算顯著。**
- 省 token：壓縮後每一問端點回報的 prompt 少 **283 token**（2266→1983，−12.5%；記憶本身 850→496，−42%）。這個差一直留到下一次壓縮。
- 回本：濃縮一次花 759 token（644 輸入＋115 輸出）。只算 token 數：759÷283≈**2.7，第 3 問起回本**；若輸出 token 照 4 倍輸入價算：（644＋460）÷283≈3.9，第 4 問起回本。端點有 prompt 快取的話，記憶前段本來就便宜，回本要更多問（這版沒量快取）。

> **審查 M6 之後**：封存摘要裡「使用者：…」那幾行機械原樣保留、不給模型改寫，所以濃縮後的摘要會比上表略長，省 token 與回本的數字**沒有重量**（重跑被使用者要關機中斷，留下一輪）。→ 09-25 已補，見下一節。

## 09-25 補：M6 之後的數字（撈 09-24 晚被中斷的重跑，沒再叫模型）

09-24 晚那次重跑（`live.py 5 ~/tmp/w3b-compact/m6 sum -m6`，已是 M6 版）被關機中斷前，**sum-1～3 已經跑完整套**（壓縮＋兩問），sum-4 還沒壓縮。09-25 LiteLLM（4000 埠）沒開，所以不再真跑，改用 [salvage_m6.py](salvage_m6.py) 從那三個 agent 家撈數字：同一份 archive 用 `plan()` 重算機械版（純函式、不叫模型），跟實際寫進記憶的比。結果 [results-m6.jsonl](results-m6.jsonl)，摘要原文 [digests-m6/](digests-m6/sum-1.txt)。

| 次 | 用了模型版？ | 封存摘要 bytes（實際／機械） | 記憶 token（實際／機械，粗估） | 每問省 | 濃縮那次 token | 行數題 | 水果題 |
|---|---|---|---|---|---|---|---|
| 1 | 是 | 1676／1986 | 840／939 | 99 | 702＋61 | 對 | 對 |
| 2 | 是 | 1841／2015 | 884／933 | 49 | 715＋111 | 對 | 對 |
| 3 | **退回**（「沒有比原摘要短」1354 ≥ 1221） | 1453／1453 | 774／774 | 0 | 565＋166（白花） | 答「不記得」 | 對 |

- **為什麼省這麼少**：這套對話裡使用者的兩段長自我介紹，佔機械摘要本體約 2／3（1148／約 1700 bytes，昨天 5 份都一樣）。M6 規定原話原樣保留，模型能壓的只剩工具與回話那 1／3。
- **每問省**：3 次平均約 50 token（用了模型版的兩次 49～99）；M6 之前是 354（記憶粗估）／283（端點實測）。端點 prompt_tokens 沒有同一次的機械版對照，不拿來比。
- **回本**：濃縮一次約 770 token。只算用到的兩次：770÷74≈第 11 問；把會退回的算進去（3 次有 1 次白花）：約第 16 問。輸出照 4 倍價算還要更後面。
- 第 3 次退回後行數題答「不記得」，跟昨天機械版那次一樣（摘要裡明明有 `read（40 行）`），不是濃縮造成的。

**結論（改判）：不值得。** M6 修掉「模型能改使用者原話」的洞之後，省下的只剩 M6 前的約 1／6，回本要十幾問，還有 1／3 機會白花一次。旗標照留、預設關（報告 §6 代裁 1「四項模型版全預設關」不變）。

## 六軸對照（[axes.md §4](../../../axes.md)）

| 軸 | 機械版 | `--summarize` | 依據 |
|---|---|---|---|
| 1 LLM 參與 | 5 | 4 | 機械不叫模型；summarize 是可選旗標、預設不叫，叫的話每段一次、有機械檢查兜底（單看那一次是 3） |
| 2 穩定 | 10/10（第一波）；這次 5/5 | 5/5 次過（不滿 10 次不給分） | 壓縮本身兩版 10/10 都成；summarize 5/5 用了模型版、0 退回；退回、模型出錯、崩在 archive 之後重跑都有單元測試 |
| 3 資源 | 4 | 4 | cpu 都 < 0.5 s；summarize 每次多送模型約 644＋115 token（300～800 那格＝4）；archive 全留（同第一波） |
| 4 快 | 5（0.03～0.04 s） | 3（0.99～1.39 s） | 指令本身的經過時間；summarize 那一秒持著 tick 鎖，agent 那格退 101 下一格再來 |
| 5 人易懂 | 4 | 4 | 模型摘要是白話、比機械摘要好讀；但要信它得翻 `sum-N.mech.txt` 或 archive 對照；開頭寫明「模型濃縮」，事件與 usage 都記 |
| 6 邊界 | 4 | 4 | 都只寫自己家（記憶、archive、log）；summarize 多一次對外 HTTP 到 llm.json 的端點，寫的路徑不變 |

最弱兩軸：summarize 的「快」（1 s 級、持鎖）與「LLM 參與」。

## 結論

L 軸從 5 掉到 4，換到的是：**壓縮後每問省 283 prompt token（−12.5%），第 3～4 問回本**；答對率 5/5 vs 4/5 方向一致但樣本太小，不當證據。
所以：**模型版預設關，只留人用的 `--summarize` 旗標**；tick 自動與申請一律不叫模型。記憶長、壓縮後還要聊很多輪的人值得手動跑一次。
