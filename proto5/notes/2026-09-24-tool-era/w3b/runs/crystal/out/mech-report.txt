route.log：20 句
落穿 20 次：沒命中 19、命中兩條以上 0、含否定詞 1、沒領隊 0

沒命中的句型（7 類；只看「沒命中」的，否定詞、命中兩條的加規則也沒用）：
- 在專案建一個{檔名}，內容寫「{引號內文}」  ×4  ← 領隊每次開同一種單
    「在專案建一個 hello.md，內容寫「你好世界」」→ t-0001 開單給 worker-1，工作流 無，驗收 check:contains、file_exists、judge：在專案根目錄建立 hello.md，內容為「你好世界」（原話：內容寫「你好世界」…
    「在專案建一個 todo.md，內容寫「買牛奶」」→ t-0005 開單給 worker-1，工作流 無，驗收 check:contains、file_exists、judge：在專案根目錄建立 todo.md，內容為「買牛奶」（原話：內容寫「買牛奶」，意思…
    「在專案建一個 idea.md，內容寫「做一個小遊戲」」→ t-0010 開單給 worker-1，工作流 無，驗收 check:contains、file_exists、judge：在專案根目錄建立 idea.md，內容為「做一個小遊戲」（原話：內容寫「做一個小…
    …另 1 句
- 把{檔名}改名成{檔名}  ×4  ← 領隊每次開同一種單
    「把 a.md 改名成 alpha.md」→ t-0002 開單給 worker-1，工作流 無，驗收 check:wf_residue、file_exists：把專案根目錄的 a.md 改名成 alpha.md：改名後專案裡看得到 alph…
    「把 b.md 改名成 beta.md」→ t-0006 開單給 worker-1，工作流 無，驗收 check:wf_residue、file_exists：把專案根目錄的 b.md 改名成 beta.md：改名後專案裡看得到 beta.…
    「把 c.md 改名成 gamma.md」→ t-0011 開單給 worker-1，工作流 無，驗收 check:wf_residue、file_exists：把專案根目錄的 c.md 改名成 gamma.md：改名後專案裡看得到 gamm…
    …另 1 句
- 數{檔名}有幾行，寫進{檔名}  ×4  ← 領隊每次開同一種單
    「數 poem.md 有幾行，寫進 poem-lines.txt」→ t-0003 開單給 worker-1，工作流 無，驗收 file_exists、judge：數專案根目錄 poem.md 有幾行，把這個行數寫進新檔 poem-lines.…
    「數 story.md 有幾行，寫進 story-lines.txt」→ t-0007 開單給 worker-1，工作流 無，驗收 file_exists、judge：數專案根目錄 story.md 有幾行，把這個行數寫進新檔 story-line…
    「數 list.md 有幾行，寫進 count.txt」→ t-0012 開單給 worker-1，工作流 無，驗收 file_exists、judge：數專案根目錄 list.md 有幾行，把這個行數寫進新檔 count.txt（原…
    …另 1 句
- 把{檔名}的第{數字}行刪掉  ×4  ← 領隊每次開同一種單
    「把 e.md 的第 2 行刪掉」→ t-0004 開單給 worker-1，工作流 無，驗收 check:contains、judge：把專案根目錄 e.md 的第 2 行刪掉，其餘每一行原文照留、順序不變（原話：把…
    「把 f.md 的第 3 行刪掉」→ t-0008 開單給 worker-1，工作流 無，驗收 check:contains、judge：把專案根目錄 f.md 的第 3 行刪掉，其餘每一行原文照留、順序不變（原話：把…
    「把 g.md 的第 1 行刪掉」→ t-0013 開單給 worker-1，工作流 無，驗收 check:contains、judge：把專案根目錄 g.md 的第 1 行刪掉，其餘每一行原文照留、順序不變（原話：把…
    …另 1 句
- 專案裡有哪些檔？  ×1
    「專案裡有哪些檔？」→ 沒對到領隊開的單
- 幫我寫一段README介紹這個專案  ×1
    「幫我寫一段 README 介紹這個專案」→ t-0009 開單給 worker-1，工作流 無，驗收 file_exists、judge：在專案根目錄建立 README.md，寫一段介紹這個專案的文字（原話：幫我寫一段…
- 把所有md檔的標題都改成大寫  ×1
    「把所有 md 檔的標題都改成大寫」→ t-0014 開單給 worker-1，工作流 無，驗收 judge：把專案根目錄所有 .md 檔裡的標題文字改成大寫（原話：把所有 md 檔的標題都…

候選規則 4 條（機械產生；不會自動生效）：
- crystal-1：在專案建一個\s*(?P<f1>[A-Za-z0-9_~./][A-Za-z0-9_~./-]*\.[A-Za-z0-9]{1,8})\s*，內容寫\s*「\s*(?P<q1>[^」]+)\s*」
    → 開單給 worker-1：在專案根目錄建立 {f1}，內容為「{q1}」（原話：內容寫「{q1}」，意思不能變）。
    例句 hit 4、miss 4，全過；回測 16 句舊句子，新吃到 0 句
- crystal-2：把\s*(?P<f1>[A-Za-z0-9_~./][A-Za-z0-9_~./-]*\.[A-Za-z0-9]{1,8})\s*改名成\s*(?P<f2>[A-Za-z0-9_~./][A-Za-z0-9_~./-]*\.[A-Za-z0-9]{1,8})
    → 開單給 worker-1：把專案根目錄的 {f1} 改名成 {f2}：改名後專案裡看得到 {f2}、看不到 {f1}，內容與改名前一模一樣（原話：…
    例句 hit 4、miss 4，全過；回測 16 句舊句子，新吃到 0 句
- crystal-3：數\s*(?P<f1>[A-Za-z0-9_~./][A-Za-z0-9_~./-]*\.[A-Za-z0-9]{1,8})\s*有幾行，寫進\s*(?P<f2>[A-Za-z0-9_~./][A-Za-z0-9_~./-]*\.[A-Za-z0-9]{1,8})
    → 開單給 worker-1：數專案根目錄 {f1} 有幾行，把這個行數寫進新檔 {f2}（原話：數 {f1} 有幾行，寫進 {f2}）。{f2} 內…
    例句 hit 4、miss 4，全過；回測 16 句舊句子，新吃到 0 句
- crystal-4：把\s*(?P<f1>[A-Za-z0-9_~./][A-Za-z0-9_~./-]*\.[A-Za-z0-9]{1,8})\s*的第\s*(?P<n1>[0-9]+)\s*行刪掉
    → 開單給 worker-1：把專案根目錄 {f1} 的第 {n1} 行刪掉，其餘每一行原文照留、順序不變（原話：把 {f1} 的第 {n1} 行刪掉…
    例句 hit 4、miss 4，全過；回測 16 句舊句子，新吃到 0 句

提案（現有規則＋候選）：/home/lorkhan/repo/simple_tools/aos/.claude/worktrees/agent-a3ba1e31b95ea8155/proto5/notes/2026-09-24-tool-era/w3b/runs/crystal/out/mech-text.json
怎麼批：先看一眼，再
  aos-team route test --file /home/lorkhan/repo/simple_tools/aos/.claude/worktrees/agent-a3ba1e31b95ea8155/proto5/notes/2026-09-24-tool-era/w3b/runs/crystal/out/mech-text.json
  aos-team route save /home/lorkhan/repo/simple_tools/aos/.claude/worktrees/agent-a3ba1e31b95ea8155/proto5/notes/2026-09-24-tool-era/w3b/runs/crystal/out/mech-text.json
不要的候選，從檔裡刪掉那條再批。
