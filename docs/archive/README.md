# 舊一代文件（歷史存檔）

這裡是 `aos init`／`aos exec` 那一代（2026-08-30 前，`core/inst` 被 `core/exec`／`wire`／`loop`／`llm`／`agent` 取代之前）的文件，只作歷史保留，不再維護。
現行文件看上層 [`docs/`](../README.md)（從 [overview.md](../overview.md)、[usage.md](../usage.md) 開始）。
